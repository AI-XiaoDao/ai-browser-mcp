# 哈希能力可用性核对（第116轮，只读结论）

> 目的：`_audit/_codec_plan_r115.md` 把 MD5/SHA/HMAC 列为"排除项"，理由是**技能资料目录里没有那些头文件**
> （`md5\md5_.h`、`jjm\include\lz4.c`、`XxHash\xxhash.hpp` 等）。本轮实测：**该理由不成立** ——
> 缺的是"技能资料副本"，而**本机已安装的类库插件里文件齐全**。

## 1. 本机实测（只读，未编译）

| 检查 | 命令（可复现） | 结果 |
|---|---|---|
| 模块目录是否存在 | `Get-ChildItem E:\HSPC\plugins\vprj_win\classlib\user` | **`yw` 存在**（另有 `piv` / `FC` / `zlib`） |
| 头文件是否齐全 | `Get-ChildItem E:\HSPC\plugins\vprj_win\classlib\user\yw -Recurse \| ? Name -match 'md5\|jjm\|XxHash'` | `\md5\md5_.h`、`\md5\md5_.cpp`、`\jjm\include\lz4.h`、`\jjm\include\lz4.c`、`\XxHash\xxhash.h`、`\XxHash\xxhash.hpp` **全部存在** |
| 项目是否已声明该模块 | 读 `AI-Fbowser-Mcp.vprj` 的 `name = …` | **`仰望模块` 已在模块表中**（与 `视窗基本类`/`FBrowser浏览器`/`yyJSON`/`SQLite数据库` 等并列） |
| 类库是否导出哈希方法 | 技能资料 `资料\类库\仰望模块\加密解密库__p.wsv` | 见下表 |

## 2. 类库导出的哈希方法（技能资料原文，行号以该文件为准）

| 方法 | 行 | 签名要点 |
|---|---|---|
| `类 MD5类_` | 57 | `@全局类 = 真`，`@视窗.外部头文件 = "md5\md5_.h"` |
| `取数据摘要_` | 60 | `<公开 静态 类型 = 文本型 注释 = "取字节集md5">` |
| `取数据摘要2_` | 77 | `<公开 静态 类型 = 文本型 注释 = "取文件md5">` |
| `取数据摘要3_` | 83 | `<公开 静态 类型 = 文本型 注释 = "取文件md5，支持大文件">` |
| `取数据摘要_XxHash_数据` | 10 | `<公开 静态 类型 = 文本型 返回值注释 = "数据为空仍会返回非0的哈希值">` |
| `取数据摘要_XxHash_文件` | 19 | `<公开 静态 类型 = 文本型>（文件不存在返回 0 值）` |
| `取数据摘要_CRC32` | 36 | `<公开 静态 类型 = 整数>` |
| `取数据MD5_字节集` | 1862 | `<公开 静态 类型 = 字节集类>` |
| `取数据HMAC_MD5_字节集` | 1882 | `<公开 静态 类型 = 字节集类>` |

## 3. 结论与下一步（**尚未验证**）

- "零新模块"这一条**成立**：模块已在 `.vprj` 里、头文件在本机。
- 但**能否编译通过**仍未验证 —— `@视窗.外部头文件` 的实际解析路径、`仰望模块` 是否需要在 IDE 里额外勾选
  子库、以及 `bcrypt.h`（走 CNG 的那几个方法）是否可用，都要靠**一次真实编译**才能定论。
- 建议动作（下一轮，**一次编译即可判定**）：在某个分派分支里临时加一行
  `变量 _probe = MD5类_.取数据摘要_ (文本到UTF8 ("abc", 假))` 之类的最小调用 → `/c` 语法自检。
  - 编译通过 ⇒ 可以新增 `browser_hash`（action: `md5` / `md5_file` / `hmac_md5` / `xxhash` / `crc32`），
    并把该探针行撤掉；
  - 编译失败 ⇒ 把**原始报错全文**记下来（缺哪个头/哪个符号），作为"本机确实不可用"的证据，届时再关闭该项。
- 纪律：本文件**不含任何"已验证可用"结论**；`_codec_plan_r115.md` 的排除理由需要按其原文修订为
  "技能资料缺副本，但本机插件目录齐全，可用性待编译判定"。
