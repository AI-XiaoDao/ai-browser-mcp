# 火山官方模块 API 技能书

> 涵盖 `火山系统API知识库\` 目录下 **67 个核心模块**的完整参考（FBrowser CEF + 火山核心库 + 社区第三方库）。
> 使用本技能书快速查找类名、方法签名、常量定义。
>
> **扩展技能书**（补充模块）：
> - 📘 [volcano-core-ext.md](volcano-core-ext.md) — 核心扩展10模块：ADO数据库、AI问答、C++正则、OpenSSL、cURL网络传输、iconv编码、JSON(jsoncpp)、HTML解析(gumbo/CSS选择器)、plog日志
> - 📘 [volcano-ext-libs.md](volcano-ext-libs.md) — 扩展库37模块：MFC/WTL界面(9)、HP-Socket网络(5)、Redis/V8/ELK/Python(4)、FTP/邮件(2)、图像/多媒体(3)、MMKV/树结构/libzip等(8)、云服务SDK(5)
> - 📘 [volcano-supplementary.md](volcano-supplementary.md) — 社区/第三方补充170+模块：MFC/WTL补充组件、ECharts图表、IGR报表、XCGUI炫彩、MiniBlink/WebView2、网络库补充(WebSocket/KCP/nanomsg等)、COM封装、STL模板、Lkuaiy系列、社区工具库
> - 📘 [yyjson-api.md](yyjson-api.md) — yyjson JSON库完整API参考

---

## 目录

### 一、FBrowser CEF 浏览器框架（8个模块）
- [FBroLib](#fbroLib) — 核心库（初始/浏览器/框架/Cookie）
- [FBroCallback](#fbrocallback) — 事件回调类
- [FBroEventControl](#fbroeventcontrol) — 浏览器事件合集
- [FBroConst](#fbroconst) — 常量/枚举
- [FBroDataType](#fbrodatatype) — 数据结构体/数组容器
- [FBroValue](#fbrovalue) — CEF值类型（V8/DOM/Dict/List）
- [FBroVip](#fbrovip) — VIP功能（指纹/高级操作/CDP）
- [FBroHelp](#fbrohelp) — 辅助设置类

### 二、火山核心库 w_\*（18个模块）
- [w_base](#w_base-w_string-w_bin) — 对象类/基础类型
- [w_string](#w_base-w_string-w_bin) — 文本操作
- [w_bin](#w_base-w_string-w_bin) — 字节集操作
- [w_array](#w_array) — 数组操作
- [w_math](#w_math) — 数学/数值转换/几何
- [w_misc](#w_misc) — 杂项工具集（时间/磁盘/文件/调试/控制台/哈希表/GUID）⭐
- [w_stream](#w_stream) — 流 I/O
- [w_muti_thread](#w_muti_thread) — 多线程/锁/原子
- [w_network](#w_network) — 网络（TCP/UDP）
- [w_sqlite3](#w_sqlite3) — SQLite3 数据库
- [w_yyjson](#w_yyjson) — JSON（yyjson）
- [w_com](#w_com) — COM 操作
- [w_res](#w_res) — 资源/GDI
- [w_startup](#w_startup) — 程序启动
- [w_exception](#w_exception) — 异常处理
- [w_device](#w_device) — 串口
- [w_multi_media](#w_multi_media) — 多媒体（声音）
- [w_service](#w_service) — Windows 服务

### 三、社区/第三方库（41个模块）
- [加密解密库](#加密解密库) — AES/MD5/CRC/XxHash
- [网络访问库](#网络访问库) — HTTP/SSL/代理
- [文件操作库](#文件操作库) — 文件属性/文本行操作
- [解压缩操作库](#解压缩操作库) — ZIP/GZip/Deflate
- [数据操作库](#数据操作库) — 剪辑板/拼接符
- [编码进制库](#编码进制库) — SIMD编码转换/进制转换
- [正则/数据匹配库](#数据匹配库) — 正则表达式
- [数据库操作](#数据库操作) — ADO数据库
- [JSON操作库](#json操作库) — yyjson简化封装
- [图像处理库](#图像处理库) — WebP编解码
- [OCR识别库](#ocr识别库) — RapidOCR/DdddOcr/NCNN
- [条码二维码库](#条码二维码库) — ZBar/qrencode
- [脚本操作库](#脚本操作库) — ChakraCore/ActiveScript JS引擎
- [语音操作库](#语音操作库) — TTS语音合成
- [日志记录库](#日志记录库) — spdlog日志
- [日期时间库](#日期时间库) — 毫秒/纳秒/UTC
- [队列操作库](#队列操作库) — 线程安全队列
- [线程操作库](#线程操作库) — 线程/线程池
- [注册表库](#注册表库) — 注册表读写
- [硬件操作库](#硬件操作库) — WMI硬件信息
- [进程权限库](#进程权限库) — 进程管理/互斥体
- [内存操作库](#内存操作库) — 远程Call/特征码
- [指针操作库](#指针操作库) — 函数指针调用
- [CMD操作库](#cmd操作库) — 命令行/匿名管道
- [FTP操作库](#ftp操作库) — FTP客户端
- [PDF操作库](#pdf操作库) — PDFium渲染
- [XML操作库](#xml操作库) — MSXML DOM
- [汉字拼音库](#汉字拼音库) — 拼音检索/分词
- [纯真IP库](#纯真ip库) — IP归属地
- [播放器库](#播放器库) — WMP播放器
- [迅雷下载库](#迅雷下载库) — 迅雷引擎下载
- [文件打印库](#文件打印库) — Excel/Word/PDF打印
- [程序综合库](#程序综合库) — 格式化/链表排序
- [组件操作库](#组件操作库) — GUI组件批量修改
- [外部组件操作库](#外部组件操作库) — 外部窗口组件操作
- [动态创建库](#动态创建库) — 动态创建GUI控件
- [动态链接库](#动态链接库) — 内存加载DLL
- [事件钩子库](#事件钩子库) — 热键/键盘鼠标钩子
- [ASM汇编库](#asm汇编库) — x86/x64汇编代码生成
- [Accessible无障碍](#accessible无障碍) — MSAA无障碍
- [数组操作库-快速排序](#数组操作库-快速排序) — 快速排序

---

## 一、FBrowser CEF 框架

### FBroLib

**包**：`FBrowser.浏览器` | **核心库文件**

#### 全局初始化 / 静态方法

| 方法 | 说明 |
|------|------|
| `FBrowser_初始化(基础设置, 初始化事件)` → 逻辑型 | 初始化 CEF，必须首先调用 |
| `FBrowser_初始化_设置V8环境默认堆栈大小(最小MB, 最大MB)` | 初始化前设置V8堆栈大小 |
| `FBrowser_关闭(结束程序=假)` | 关闭全部浏览器；结束程序=真则直接结束进程 |
| `FBrowser_消息循环_运行()` / `执行()` / `退出()` / `设置系统模式(系统模式)` | 主消息循环；`运行()`=阻塞模式，`执行()`=单次迭代（手动循环用），`退出()`=退出循环，`设置系统模式()`=设置OS模态循环 |
| `FBrowser_取版本号()` → 文本型 | 模块版本号 |
| `FBrowser_取初始化缓存目录()` → 文本型 | 取出初始化设置的缓存真实路径 |
| `FBrowser_进程_取当前进程类型()` → 整数 | 返回当前进程类型（参考：进程类型.*）|
| `FBrowser创建事件智能指针(数据类型)` → 类_FBrowser_事件智能指针 | 创建事件智能指针封装 |
| `FBrowser创建类指针(类)` → 类_FBrowser_事件智能指针 | 动态创建类指针（用后需手动释放） |
| `FBrowser初始化控制.启用自带调试提示(真)` / `关闭自带调试提示(真)` | 框架调试提示开关 |
| `FBrowser_设置程序DPI模式(模式)` | 设置DPI感知模式 |
| `FBrowser_内存_压缩清理()` | 手动清理框架内存占用 |

#### JS交互（静态）

| 方法 | 说明 |
|------|------|
| `FBrowser_JS交互_注册(JS函数名, 退出函数名, 事件)` | 注册全局 `window.cefQuery()` 绑定 |
| `FBrowser_JS交互_删除(JS函数名)` | 删除已注册的JS交互函数 |

#### V8扩展（静态，渲染进程用）

| 方法 | 说明 |
|------|------|
| `FBrowser_V8_注册JS扩展(名称标识, JS代码, V8处理程序)` | 在渲染进程注册原生JS扩展 |
| `FBrowser_V8环境_取运行环境()` → 类_FBrowser_V8环境 | 取当前线程V8运行环境 |
| `FBrowser_V8环境_取当前环境()` → 类_FBrowser_V8环境 | 取当前上下文V8环境 |

#### 浏览器管理（静态）

| 方法 | 说明 |
|------|------|
| `FBrowser_创建浏览器(URL, 窗口信息, 浏览器设置, 请求环境, 额外信息, 浏览器事件, 禁用事件, 标识)` → 逻辑型 | 创建新浏览器（异步） |
| `FBrowser_创建浏览器_同步(URL, 窗口信息, 浏览器设置, 请求环境, 额外信息, 浏览器事件, 禁用事件, 标识)` → 类_FBrowser_浏览器 | 同步创建（必须在UI线程且非浏览器事件中调用） |
| `FBrowser_创建后台浏览器(URL, 浏览器设置, 请求环境, 额外信息, 浏览器事件, 禁用事件)` → 逻辑型 | 创建无窗口后台浏览器 |
| `FBrowser_创建后台浏览器_同步(URL, ...)` → 类_FBrowser_浏览器 | 同步创建后台浏览器（需UI线程任务投递） |
| `FBrowser_浏览器_通过ID取浏览器(ID)` → 类_FBrowser_浏览器 | 通过整数ID取浏览器实例 |
| `FBrowser_浏览器_通过序号取浏览器(序号)` → 类_FBrowser_浏览器 | 通过内置清单索引取浏览器 |
| `FBrowser_浏览器_通过用户标识取浏览器(标识)` → 类_FBrowser_浏览器 | 通过用户标识字符串取浏览器（创建时传入） |
| `FBrowser_浏览器_通过窗口句柄取浏览器(句柄)` → 类_FBrowser_浏览器 | 通过窗口句柄取浏览器 |
| `FBrowser_浏览器_取数量()` → 整数 | 当前所有浏览器数量 |
| `FBrowser_浏览器_取ID清单()` → 类_FBrowser_列表值 | 所有浏览器ID列表 |
| `FBrowser_浏览器_取用户标识清单()` → 类_FBrowser_列表值 | 所有浏览器用户标识列表 |

#### 服务器管理（静态）

| 方法 | 说明 |
|------|------|
| `FBrowser_服务器_创建(IP, 端口, 最大连接数, 服务器事件)` | 创建内嵌HTTP/WS服务器 |

#### 任务运行器（静态）

| 方法 | 说明 |
|------|------|
| `FBrowser_任务运行器_投递任务(线程ID, 任务回调)` → 逻辑型 | 投递任务到指定线程（常用：线程ID.UI） |
| `FBrowser_任务运行器_投递任务_延迟(线程ID, 延迟ms, 回调)` → 逻辑型 | 延迟投递任务 |
| `FBrowser_任务运行器_取当前()` → 类_FBrowser_任务运行器 | 取当前线程的运行器 |
| `FBrowser_任务运行器_取指定线程(线程ID)` → 类_FBrowser_任务运行器 | 取指定线程的运行器 |
| `FBrowser_任务运行器_是否指定线程上调用(线程ID)` → 逻辑型 | 判断当前是否在指定线程 |
| `类_FBrowser_任务运行器.投递任务(回调)` / `投递延时任务(延时,回调)` | 实例方法（非静态）；`是否为当前/指定线程()` |

#### URL请求（静态）

| 方法 | 说明 |
|------|------|
| `FBrowser_创建URL请求(请求, 请求环境, URL请求事件, 标识)` | 创建独立全局URL请求 |

#### 类_FBrowser_服务器（实例）

| 方法 | 说明 |
|------|------|
| `FBrowser_服务器_创建(地址, 端口, 最大连接数, 服务处理事件)` | 静态，创建内嵌HTTP/WS服务器 |
| `是否为空()`, `置空()` | 状态/释放 |
| `关闭()` | 停止服务器并关闭专用服务器线程 |
| `是否运行中()` → 逻辑型 | 是否正在运行并接受连接 |
| `取服务器地址()` → 文本型 | 返回含端口的服务器地址 |
| `是否存在连接()` / `是否有效连接(连接ID)` → 逻辑型 | 连接状态查询 |
| `发送Http200响应(连接ID, 连接类型, 数据指针, 数据大小)` | 发送200 OK响应 |
| `发送Http404响应(连接ID)` | 发送404响应 |
| `发送Http500响应(连接ID, 错误文本)` | 发送500响应 |
| `发送HttpResponse(连接ID, 响应码, 响应类型, 内容长度, 额外头)` | 发送自定义HTTP响应 |
| `发送原始数据(连接ID, 数据指针, 数据大小)` | 直接发送原始字节 |
| `关闭连接(连接ID)` | 关闭指定连接 |
| `发送WebSocket数据(连接ID, 数据指针, 数据大小)` | 发送WebSocket帧 |
| `取任务运行器()` → 类_FBrowser_任务运行器 | 取服务器专用线程的运行器 |

#### 自定义方案（静态）

| 方法 | 说明 |
|------|------|
| `FBrowser_自定义方案_注册(方案名, 标识, 资源处理器)` | 注册自定义scheme（如`FBrowser://`） |
| `FBrowser_自定义方案_清理()` | 清除所有自定义scheme注册 |

#### 类_FBrowser_浏览器（实例）

**空值/null检查**：`是否为空()`→逻辑型, `是否相同(对比浏览器)`→逻辑型

**导航**：`后退()`, `前进()`, `重新载入()`, `重新载入_忽略缓存()`, `停止载入()`
**状态**：`取ID()`→整数, `是否读取中()`→逻辑型, `可否后退()`/`可否前进()`, `是否已关闭()`, `是否为弹窗()`, `是否有文档()`, `是否浏览器视图()`, `是否存在开发者工具()`
**框架**：`取主框架()`→类_FBrowser_框架, `取当前焦点框架()`, `取框架_ID(ID)`, `取框架_名称(名称)`
**取框架清单**：`取框架ID()`→FBrowser_文本数组, `取框架名称()`→FBrowser_文本数组（可转火山文本数组）
**操作**：`关闭浏览器(立即=假, 置空=假)`（立即=真强制关闭）, `尝试关闭浏览器(置空)`→逻辑型, `置焦点(焦点)`, `取缩放级别()`/`置缩放级别(级别)`, `置页面静音(静音)`/`是否页面静音()`, `置自动调整大小(启用, x, y, w, h)`
**窗口**：`取窗口句柄()`→变整数, `取打开者窗口句柄()`→变整数, `取父窗口句柄()`, `置父窗口(句柄)`, `移动窗口(x,y,w,h,重画)`, `显示隐藏窗口(显示隐藏)`, `取窗口标题()`→文本型, `取窗口属性(风格类型)`/`置窗口属性(风格类型,风格)`
**下载/打印/查找**：`开始下载(地址)`, `下载图片(地址, 图标?, 最大尺寸, 忽略缓存, 回调)`, `打印()`, `打印为PDF(路径, 设置, 回调)`, `查找(文本, 向前, 区分, 下一个)`/`停止查找(选项)`
**DevTools**：`打开开发者工具(标题, 父窗口, x, y, w, h, 浏览器设置, 元素坐标, 浏览器事件, 禁用事件)`（全部参数可选）, `关闭开发者工具()`, `是否为开发者工具()`→逻辑型, `是否存在开发者工具()`→逻辑型
**文件对话框**：`打开对话框(模式, 标题, 默认路径, 过滤器, 回调)`—模式参考`文件对话框.*`
**输入**：`发送按键事件(事件)`, `发送鼠标点击事件(类型, 事件, 点击, 次数)`, `发送鼠标移动事件(事件, 离开)`, `发送鼠标滚轮事件(事件, dx, dy)`, `发送触摸事件(事件)`
**代理/缓存**：`设置代理(地址, 账号, 密码)`, `清空代理()`, `清理缓存(源=空, 对象=空, 类型=空, 回调)`
**进程消息**：`进程间消息_发送数据_到全部渲染进程(消息名, 消息)`, `进程间消息_发送数据_到指定渲染进程(ID, 名, 消息)`, `进程间消息_取渲染进程数量()`/`取渲染进程ID清单()`
**其他**：`取请求环境()`→类_FBrowser_请求环境, `取主浏览器()`, `取VIP控制器()`→VIP, `取开发者DOM()`→VIP DOM, `取用户标识()`→文本型, `取额外数据()`→类_FBrowser_字典值, `取窗口运行风格()`→整数
**填表框架**：`取主填表框架()` / `取焦点填表框架()` / `取填表框架_ID(ID)` / `取填表框架_名称(名称)` → 类_FBrowser_填表框架；`取框架ID(框架)` → 按框架取填表框架

#### 类_FBrowser_基础框架（框架基类，`类_FBrowser_框架`和`类_FBrowser_填表框架`均继承此）

**通用方法**：`是否为空()`, `置空()`, `是否有效()`, `是否为主框架()`, `是否为焦点框架()`, `取地址()`, `取框架名()`, `取框架ID()`, `执行JS代码(代码, 地址, 行)`, `执行JS代码_带返回值(代码, 地址, 行, 回调)`, `取浏览器()`, `取填表框架()`, `取框架()`

#### 类_FBrowser_框架（继承自类_FBrowser_基础框架）

**导航**：`载入地址(URL)`, `载入请求(请求)`（支持GET/POST+自定义协议头）
**异步取内容**：`取源码_异步(回调)`, `取文本_异步(回调)`
**编辑命令**：`撤销()/恢复()/剪切()/复制()/粘贴()/删除()/全选()`; 额外: `源码视图()`
**进程通信**：`发送进程消息(进程类型, 进程消息)`
**V8/DOM**：`取V8环境()`→类_FBrowser_V8环境(渲染进程), `访问DOM对象(DOM回调)`(渲染进程)
**URL请求**：`创建URL请求(请求, URL请求事件, 标识)` → 以框架为上下文创建URL请求
**其他**：`取浏览器()`→类_FBrowser_浏览器, `取父框架()`→类_FBrowser_框架

#### 类_FBrowser_请求 / 响应 / POST数据

**请求**：`取地址()`/`置地址()`, `取类型()`/`置类型()` (GET/POST), `设置(地址, 方式, POST数据, 协议头)`, `置来路(地址, 策略)`, `取来路地址()`/`取来路类型()`, `取POST数据()`/`置POST数据()`, `取协议头数据()`/`置协议头数据()`, `置协议头数据_数组(头数组, 删除其他)`, `取协议头_名称(名称)`→文本型, `置协议头_名称(名, 值, 覆盖)`, `删除协议头数据(关键字)`, `取标识()`/`设置标识(标识)`, `取资源类型()`, `取过渡类型()`→长整数, `取ID()`→长整数, `获取地址_首件cookie()`/`设置地址_首件cookie(地址)`
**来路策略常量**：`默认`, `同源发送完整请求`, `异源合并请求`, `不改变请求`, `原点请求`, `跨源清除请求`, `删除请求到源`, `清除请求`
**POST数据**：`创建()`, `是否只读()`, `是否包含元素()`, `取元素个数()`, `取元素()`→POST元素数组, `增加元素(元素)`, `移除元素(元素)`, `移除所有元素()`
**POST元素**：`创建()`, `是否只读()`, `置空数据()`, `置数据_文件(文件名)`, `置数据(字节集)`, `置数据_文本(文本)`, `取类型()`, `取文件()`, `取数据大小()`, `取字节集数据(大小)`, `取文本数据(是否UTF8)`
**响应**：`取状态()`/`置状态()`, `取状态文本()`/`置状态文本()`, `取MIME类型()`/`置MIME类型()`, `取字符集()`/`置字符集()`, `取错误()`/`置错误()`, `取地址()`/`置地址()`, `取协议头数据()`→双文本数组, `置协议头数据(双文本)`, `置协议头数据_数组(数组, 删除其他)`, `取协议头_名称(名称)`→文本型, `置协议头_名称(名称,值,覆盖)`, `删除协议头数据(关键字)`, `是否只读()`

#### 类_FBrowser_Cookie管理器

| 方法 | 说明 |
|------|------|
| `FBrowser_Cookie管理器_取全局()` → Cookie管理器 | 静态 |
| `取全部Cookie(Cookie回调)` | 异步，逐个访问 |
| `取地址Cookie(地址, 取全部, 回调)` | 按URL取 |
| `置Cookie(地址, Cookie数据)` | 地址须完整URL |
| `删除Cookie(地址, 名)` | 地址和名为空=删全部 |
| `刷新Cookie()` | 写入磁盘 |

#### 类_FBrowser_请求环境

| 方法 | 说明 |
|------|------|
| `FBrowser_请求环境_取全局()` → 请求环境 | 静态 |
| `创建(配置)`, `创建_其他(其他环境)` | 独立请求环境 |
| `是否为全局()`, `是否相同(其他环境)`, `是否为分享(其他环境)` | 状态判断 |
| `取缓存路径()`, `取Cookie管理器()` | 缓存/Cookie |
| `置首选项(值, 名称, 错误)` | Chromium首选项 |
| `VIP_高级_载入插件路径/安装CRX插件包/卸载插件` | VIP插件管理 |
| `VIP_高级_取插件地址/路径/名(插件ID)` → 文本型 | VIP插件信息 |

#### 全局辅助

- `FBrowser_Parser_Base64编码/解码(文本)` → 文本型/字节集类
- `FBrowser_Parser_取数据URI(mime, 数据)` → 文本型
- `FBrowser_Parser_URI编码/解码(文本, 选项)` → 文本型
- `FBrowser_Parser_解析JSON(json, opt)` → 类_FBrowser_值
- `FBrowser_Parser_字节值解析为JSON(字节集, opt)` → 类_FBrowser_值
- `FBrowser_Parser_写入JSON(值, opt)` → 文本型
- `FBrowser_清理全局缓存(源, 对象, 类型, 回调)` — 同浏览器实例的`清理缓存`，但无需浏览器实例

---

### FBroCallback

**包**：`FBrowser.回调`

#### 事件回调类（被通知方，创建后通过事件智能指针传入）

| 类 | 关键虚拟方法 | 用途 |
|----|-------------|------|
| `类_FBrowser_字符串回调` | `回调(文本)` | 取源码/取文本 |
| `类_FBrowser_JS回调` | `回调(返回结果)` | JS返回值。`结果.取整数值(1)`=类型, `结果.取文本值(2)`=值 |
| `类_FBrowser_Cookie回调` | `开始()`, `访问(Cookie数据, 当前项, 总数, 删除当前cookie)`→逻辑型, `结束()` | Cookie遍历（逐个回调，访问返回真继续） |
| `类_FBrowser_下载图片回调` | `图片下载完成(图片链接, 状态码, 图片)` | 下载图片完成 |
| `类_FBrowser_清理缓存回调` | `完成(是否成功)` | 清理缓存完成 |
| `类_FBrowser_打印为PDF回调` | `即将完成打印(路径, 是否成功)` | 打印PDF完成通知 |
| `类_FBrowser_打开文件对话框回调` | `即将关闭文件对话框(文件路径)` | 文件对话框关闭（单文件为文本，多文件为逗号分隔） |
| `类_FBrowser_任务回调` | `执行()` | UI线程任务投递执行 |
| `类_FBrowser_DOM回调` | `回调(DOM文档)` | 渲染进程DOM文档访问结果 |
| `类_FBrowser_V8处理程序` | `开始(标识ID)`, `结束(标识ID)`, `执行(标识ID, 名字, 项目, 参数清单, 设置返回V8值, 返回异常文本)` → 逻辑型 | JS原生函数注册后被JS调用触发 |
| `类_FBrowser_V8存取器` | `开始(标识ID)`, `结束(标识ID)`, `获取(标识ID, 名, 对象, 设置返回值, 返回异常文本)`→逻辑型, `设置(标识ID, 名, 对象, 值, 返回异常文本)`→逻辑型 | V8 属性 getter/setter |
| `类_FBrowser_V8拦截器` | `开始/结束`, `获取_名(...)`, `设置_名(...)`, `获取_索引(...)`, `设置_索引(...)` | V8 通用拦截器（属性名+索引级别） |
| `类_FBrowserVIP_通用回调` | `数据回调(...)`, `列表数据回调(...)` | VIP截图/JS结果 |

#### 同步辅助类（重要）

| 类 | 关键方法 | 用途 |
|----|---------|------|
| `类_FBrowser_同步辅助类` | `创建()`, `销毁()`, `等待()`, `停止等待()`, `重置()`, `是否有效()`→逻辑型, `绑定(指针)`, `取指针()`→变整数 | 将异步回调转同步等待（不能用于浏览器事件中） |
| | `置文本型(文本)`, `取文本型()`→文本型 | 文本数据传递 |
| | `置整数(整数)`, `取整数()`→整数 | 整数数据传递 |
| | `置双精度小数型(小数)`, `取双精度小数型()`→小数 | 小数数据传递 |
| | `置逻辑型(逻辑)`, `取逻辑型()`→逻辑型 | 逻辑数据传递 |
| | `置框架(框架)`, `取框架()`→类_FBrowser_框架 | 框架引用传递 |
| | `置浏览器(浏览器)`, `取浏览器()`→类_FBrowser_浏览器 | 浏览器引用传递 |
| | `取字节集()`→字节集类, `添加字节集(字节集)` | 字节集累加/读取 |
| | `是否存在文本值/整数值/逻辑值/双精度小数值/字节集值()`→逻辑型 | 检查值存在（在等待后判断哪种类型有值） |

#### 指令回调类（控制方，调用继续/取消等）

| 类 | 关键方法 | 用途 |
|----|---------|------|
| `类_FBrowser_授权回调` | `继续(用户名, 密码)` | HTTP认证 |
| `类_FBrowser_即将下载回调` | `继续(路径, 弹出对话框)` | 设置下载路径 |
| `类_FBrowser_正在下载回调` | `取消()`, `暂停()`, `恢复()` | 下载控制 |
| `类_FBrowser_JS对话框回调` | `继续(成功, 输入文本)` | JS alert/confirm/prompt响应 |
| `类_FBrowser_JS交互回调` | `成功(响应文本)`, `失败(错误码, 文本)` | cefQuery响应 |
| `类_FBrowser_对话框回调` | `继续(文件路径)`, `取消()` | 文件选择对话框 |
| `类_FBrowser_回调` | `继续()`, `取消()` | 通用CEF回调 |
| `类_FBrowser_运行命令菜单回调` | | 自定义菜单运行回调 |
| `类_FBrowser_资源读取回调` | | 资源处理器读取回调 |
| `类_FBrowser_资源忽略回调` | | 资源处理器忽略回调 |
| `类_FBrowser_选择证书回调` | `选择(证书)` | SSL客户端证书选择回调 |
| `类_FBrowser_媒体接入回调` | `是否为空()`, `继续(允许许可)`, `取消()` | 媒体访问许可响应 |
| `类_FBrowser_权限提示回调` | `是否为空()`, `继续(结果标识)` | 权限提示响应 |
| `类_FBrowser_快捷菜单回调` | `是否为空()`, `继续(命令ID, 事件标识)`, `取消()` | 快捷菜单运行回调 |
| `类_FBrowser_插件资源回调` | `是否为空()`, `继续(读取流)`, `取消()` | 扩展插件资源回调 |

---

### FBroEventControl

**包**：`FBrowser.事件控制`

#### 类_FBrowser_应用事件（全局生命周期，`类_FBrowser_应用事件`）

| 方法 | 说明 |
|------|------|
| `浏览器_初始化完毕` | 浏览器主进程初始化完成，可开始创建浏览器 |
| `执行关闭完毕(是否结束程序 逻辑型类)` | **单独线程执行**，所有资源释放完毕；可修改该参数为真直接结束进程 |
| `请求环境初始化完毕(请求环境)` | 请求环境/缓存目录初始化完成（非全局环境） |
| `即将处理命令行(进程类型, 命令行)` | 可修改CEF命令行参数（禁用GPU/启用摄像头等） |
| `注册自定义方案(方案)` | 注册自定义scheme（如`FBrowser://`） |
| `获取默认事件(浏览器, 地址, 用户额外配置)` | 谷歌模式下内置功能创建浏览器时回调；可通过`用户额外配置.置事件/置标识`控制 |
| `浏览器_即将启动子进程(命令行)` | 子进程启动前通知 |
| `浏览器_即将启动消息调度(延迟时间)` | 消息循环调度前通知 |

**渲染进程事件**（只在渲染进程中触发）：

| 方法 | 说明 |
|------|------|
| `渲染_即将初始化WebKit()` | 渲染进程WebKit初始化前，可注册JS扩展 |
| `渲染_浏览器创建(浏览器, 额外信息)` | 渲染进程中的浏览器创建 |
| `渲染_即将销毁浏览器(浏览器)` | 渲染进程浏览器销毁前 |
| `渲染_即将创建V8环境(浏览器, 框架, V8环境)` | ⭐渲染进程为每个frame创建V8，可注册V8函数/存取器 |
| `渲染_即将释放V8环境(浏览器, 框架, V8环境)` | V8环境释放前 |
| `渲染_即将捕获异常(浏览器, 框架, V8环境, V8异常, V8堆栈踪迹)` | JS异常捕获 |
| `渲染_焦点节点改变(浏览器, 框架, DOM节点)` | DOM焦点节点变化 |
| `渲染_收到消息(浏览器, 框架, 源进程, 消息)` → 逻辑型 | 渲染进程收到主进程IPC消息 |
| `渲染_载入状态被改变(浏览器, 是否读取中, 可后退, 可前进)` | 渲染进程加载状态变化 |
| `渲染_载入开始(浏览器, 框架, 过渡类型)` | 渲染进程页面开始加载 |
| `渲染_载入结束(浏览器, 框架, 状态码)` | 渲染进程页面加载完成 |
| `渲染_载入错误(浏览器, 框架, 错误代码, 错误文本, 失败地址)` | 渲染进程加载错误 |

#### 类_FBrowser_浏览器事件（每个浏览器实例一个）⭐最核心

**生命周期**：
- `浏览器_创建完毕(浏览器, 额外信息)` ⭐ 最常用入口；浏览器完全创建后触发
- `浏览器_即将打开新窗口(浏览器, 框架, 弹窗ID, 地址, 目标框架名称, 目标类型, 用户手势, 弹出功能, 窗口信息可设, 浏览器设置可设, 无JS访问可设, 用户额外配置可设)` → 逻辑型 | 弹窗拦截与参数设置；`用户额外配置.置事件/置标识`可控制新窗口行为
- `浏览器_打开新窗口失败(浏览器, 弹窗ID)` | 弹窗被阻止后通知
- `浏览器_即将打开开发者窗口(浏览器, 窗口信息可设, 浏览器设置可设, 使用默认窗口可设, 用户额外配置可设)` | 谷歌模式下非用户命令打开DevTools
- `浏览器_即将关闭(浏览器)` | 浏览器窗口即将关闭
- `浏览器_执行关闭(浏览器)` → 逻辑型 | 浏览器正在关闭；返回真阻止
- `浏览器_渲染意外终止(浏览器, 状态, 错误码, 错误字符串)` | 渲染进程崩溃
- `浏览器_渲染视图(浏览器)` | 渲染进程视图就绪
- `浏览器_插件意外崩溃(浏览器, 插件路径)` | 扩展插件崩溃

**导航/加载**：
- `浏览器_即将导航(浏览器, 框架, 请求, 用户动作, 是否重定向)` → 逻辑型 | 返回真阻止导航
- `浏览器_载入开始(浏览器, 框架, 过渡类型)` | 框架开始加载
- `浏览器_载入结束(浏览器, 框架, 状态码)` | 框架加载完成
- `浏览器_载入错误(浏览器, 框架, 错误码, 错误文本, 失败地址)` | 框架加载错误
- `浏览器_载入状态被改变(浏览器, 载入中, 是否能后退, 是否能前进)` | 加载状态变化
- `浏览器_地址被改变(浏览器, 框架, 地址)` | 地址栏URL变化
- `浏览器_标题被改变(浏览器, 标题)` | 页面标题变化
- `浏览器_加载进度被改变(浏览器, 进度)` | 0-100 加载进度
- `浏览器_从标签打开地址(浏览器, 框架, 目的地址, 目的配置, 用户行为)` → 逻辑型 | 标签页URL请求

**资源请求**：
- `浏览器_即将加载资源(浏览器, 框架, 请求, 回调)` → 逻辑型 | 返回真=阻止加载
- `浏览器_获取资源处理器(浏览器, 框架, 请求, 设置资源处理器)` | 替换/自定义资源响应
- `浏览器_获取资源过滤器(浏览器, 框架, 请求, 响应, 设置资源过滤器)` | 修改响应体数据
- `浏览器_重定向资源(浏览器, 框架, 请求, 响应, 新地址可修改)` | 收到重定向
- `浏览器_响应资源(浏览器, 框架, 请求, 响应)` → 逻辑型 | 收到响应头
- `浏览器_资源加载完毕(浏览器, 框架, 请求, 响应, 状态, 资源大小)` | 单资源加载完成
- `浏览器_处理协议请求(浏览器, 框架, 请求, 允许操作系统执行可设)` | 非标准协议处理

**帧（Frame）生命周期**：
- `浏览器_即将创建框架(浏览器, 框架)` | OnFrameCreated
- `浏览器_即将连接框架(浏览器, 框架, 再次连接)` | OnFrameAttached
- `浏览器_即将拆离框架(浏览器, 框架)` | OnFrameDetached
- `浏览器_即将改变主框架(浏览器, 旧框架, 新框架)` | OnMainFrameChanged
- `浏览器_即将创建主框架Document(浏览器)` | OnDocumentAvailableInMainFrame

**下载**：
- `浏览器_可下载(浏览器, 地址, 请求方法)` → 逻辑型 | 返回假取消下载
- `浏览器_即将下载(浏览器, 下载, 推荐名, 下载回调)` → 逻辑型 | 设置下载路径（`下载回调.继续(路径, 弹出对话框)`）
- `浏览器_正在下载(浏览器, 下载, 下载回调)` | 下载进度更新；`下载.取下载百分比/已下载长度/总长度/开始时间/结束时间/地址/内容描述/MIME类型/存储位置`

**菜单/快捷菜单**：
- `浏览器_即将打开菜单(浏览器, 框架, 菜单环境, 菜单模式)` | 右键菜单；`菜单模式.添加子菜单/添加菜单/设置快捷键`添加自定义菜单
- `浏览器_菜单被调用(浏览器, 框架, 菜单环境, 菜单模式, 运行命令菜单回调)` → 逻辑型
- `浏览器_菜单被点击(浏览器, 框架, 菜单环境, 命令ID, 事件标识)` → 逻辑型 | 处理自定义菜单命令
- `浏览器_菜单被关闭(浏览器, 框架)` | 菜单关闭
- `浏览器_即将运行快捷菜单(浏览器, 框架, 位置, 尺寸, 编辑状态标识, 回调)` → 逻辑型 | 输入框快捷菜单
- `浏览器_即将运行快捷菜单命令(浏览器, 框架, 命令ID, 事件标识)` → 逻辑型
- `浏览器_即将取消快捷菜单(浏览器, 框架)`

**JS对话框**：
- `浏览器_JS即将打开对话框(浏览器, 源地址, 对话框类型, 消息文本, 默认提示文本, 回调, 禁止消息可设)` → 逻辑型 | JS alert/confirm/prompt
- `浏览器_JS即将打开离开对话框(浏览器, 消息文本, 重载, 回调)` → 逻辑型 | onbeforeunload
- `浏览器_JS重置对话框(浏览器)` | 对话框状态重置
- `浏览器_JS对话框关闭(浏览器)` | 对话框关闭

**键盘/焦点**：
- `浏览器_按下某键(浏览器, 按键事件, 系统事件, 是否快捷键)` → 逻辑型 | 键盘按下前
- `浏览器_按下某键后(浏览器, 按键事件, 系统事件)` → 逻辑型 | 键盘按下后
- `浏览器_即将失去焦点(浏览器, 下一个)` | 焦点即将离开
- `浏览器_请求焦点(浏览器, 源类型)` → 逻辑型 | 焦点请求
- `浏览器_收到焦点(浏览器)` | 获得焦点

**对话框/文件选择**：
- `浏览器_即将打开对话框(浏览器, 模式, 标题, 默认路径, 过滤器, 扩展名, 接受类型, 对话框回调)` → 逻辑型 | 文件选择对话框
- `浏览器_控制台消息(浏览器, 日志级别, 消息, 来源, 行号)` → 逻辑型 | JS控制台输出

**SSL/证书/认证**：
- `浏览器_请求证书错误(浏览器, 错误码, 目标地址, SSL信息, 请求回调)` → 逻辑型 | SSL证书错误
- `浏览器_选择客户端证书(浏览器, 是否为代理, 主机地址, 端口, X509证书清单, 选择证书回调)` → 逻辑型 | 客户端证书选择
- `浏览器_获得需授权证书(浏览器, 源地址, 是否为代理, 主机, 端口, 域名, 认证方案, 授权回调)` → 逻辑型 | HTTP 401认证

**离屏渲染**：
- `离屏渲染_获取根屏幕矩形(浏览器, 矩形可设)` → 逻辑型 | 返回根屏幕矩形
- `离屏渲染_获取视图矩形(浏览器, 矩形可设)` | 获取视图区域
- `离屏渲染_获取屏幕点(浏览器, viewX, viewY, screenX可设, screenY可设)` → 逻辑型 | 视图坐标→屏幕坐标
- `离屏渲染_获取窗口信息(浏览器, 屏幕信息可设)` → 逻辑型 | 获取屏幕信息
- `离屏渲染_即将显示弹窗(浏览器, 显示)` | 弹窗显示通知
- `离屏渲染_移动调整弹窗(浏览器, 矩形)` | 弹窗位置尺寸调整
- `离屏渲染_将被绘制(浏览器, 类型, 矩形清单, 缓存指针, 宽, 高)` | 软件绘制
- `离屏渲染_将被加速绘制(浏览器, 类型, 矩形清单, 加速绘制信息)` | GPU加速绘制
- `离屏渲染_开始拖拽(浏览器, 拖拽数据, 操作类型, x, y)` → 逻辑型 | 拖拽开始
- `离屏渲染_更新拖动光标(浏览器, 操作)` | 拖拽光标更新
- `离屏渲染_滚动偏移量改变(浏览器, x, y)` | 页面滚动偏移变化
- `离屏渲染_IME范围改变(浏览器, 选择范围, 字符范围)` | 输入法IME范围变化
- `离屏渲染_文本选择改变(浏览器, 选择文本, 选择范围)` | 文本选择变化
- `离屏渲染_虚拟键盘请求(浏览器, 输入类型)` | 虚拟键盘请求

**音频流捕获**：
- `浏览器_获取音频参数(浏览器, 参数可设)` → 逻辑型 | 设置音频捕获参数
- `浏览器_即将启动音频流(浏览器, 参数, 通道数)` | 音频捕获开始
- `浏览器_收到音频流包(浏览器, 数据指针, 帧数, pts)` | 音频数据回调
- `浏览器_即将结束音频流(浏览器)` | 音频捕获结束
- `浏览器_音频流出现错误(浏览器, 错误信息)` | 音频捕获出错

**媒体权限**：
- `浏览器_即将改变媒体访问(浏览器, 包含视频访问, 包含音频访问)` | 媒体访问状态变化
- `浏览器_即将请求媒体访问许可(浏览器, 框架, 请求源, 请求许可, 回调)` → 逻辑型 | 媒体权限请求
- `浏览器_即将显示许可提示(浏览器, 提示ID, 请求源, 请求许可, 回调)` → 逻辑型 | 权限提示显示
- `浏览器_即将关闭许可提示(浏览器, 提示ID, 结果标识)` | 权限提示关闭

**其他**：
- `浏览器_收到消息(浏览器, 框架, 源进程, 消息)` → 逻辑型 | 主进程收到IPC消息
- `浏览器_全屏模式被改变(浏览器, 全屏)` | 全屏切换
- `浏览器_网页图标被改变(浏览器, 图标地址列表)` | favicon变更
- `浏览器_光标被改变(浏览器, 光标句柄, 类型, 光标信息)` → 逻辑型 | 光标样式变化
- `浏览器_工具栏被改变(浏览器, 内容)` → 逻辑型 | 工具栏文本
- `浏览器_状态栏被改变(浏览器, 内容)` | 状态栏文本
- `浏览器_自动调整尺寸(浏览器, 高度, 宽度)` → 逻辑型 | 页面自动调整大小
- `浏览器_查找返馈(浏览器, 标识ID, 数量, 选择位置, 顺序, 最后更新)` | 页内查找结果
- `浏览器_拖拽进入(浏览器, 拖拽数据, 拖动类型)` → 逻辑型 | 文件拖拽进入
- `浏览器_拖拽区域改变(浏览器, 框架, 拖拽位置)` | 可拖拽区域变化
- `浏览器_即将执行Chrome命令(浏览器, 命令ID, 配置)` → 逻辑型 | Chrome内置命令（`浏览器_即将运行Chrome命令` 的别名）
- `浏览器_JS请求存储配额(浏览器, 源地址, 新大小, 请求回调)` → 逻辑型 | JS请求存储空间

**离屏渲染（浏览器实例方法，主动调用）**：
- `离屏渲染_离屏渲染被禁用()` → 逻辑型 | 检查窗口渲染是否禁用
- `离屏渲染_通知已被调整大小()` | 通知浏览器widget大小变化
- `离屏渲染_通知已被隐藏(隐藏)` | 通知浏览器隐藏/显示
- `离屏渲染_通知屏幕信息被改变()` | 通知屏幕信息（DPI等）变化
- `离屏渲染_使视图无效(绘制元素类型)` | 使视图无效并触发重绘
- `离屏渲染_取帧率()` / `离屏渲染_置帧率(帧率)` | 获取/设置离屏渲染帧率（1-60fps，默认30）
- `离屏渲染_IME置组成(文本, 下划线组成, 代替范围, 选择范围)` | 设置IME组合文本
- `离屏渲染_IME置交互文本(文本, 代替范围, 相对光标位置)` | 完成IME组合并插入文本
- `离屏渲染_IME完成组合文本(保持选择)` | 应用当前组合文本
- `离屏渲染_IME取消组合()` | 取消当前IME组合
- `离屏渲染_拖动进入(拖拽数据, 鼠标事件, 操作类型)` | 拖拽进入webview
- `离屏渲染_拖动移动(鼠标事件, 操作类型)` | 拖拽移动
- `离屏渲染_拖动离开()` | 拖拽离开webview
- `离屏渲染_拖动放下(鼠标事件)` | 拖拽放下
- `离屏渲染_拖动结束位置(x, y, 操作类型)` | 拖拽结束
- `离屏渲染_拖动系统结束()` | 系统拖拽完成

#### 类_FBrowser_JS交互事件

| 方法 | 说明 |
|------|------|
| `即将查询(浏览器, 框架, 查询ID, 请求文本, persistent, JS交互回调)` → 逻辑型 | `window.cefQuery()` 调用时触发；返回真=已处理，需调用`JS交互回调.成功(文本)`或`.失败(码,文本)` |
| `即将取消查询(浏览器, 框架, 查询ID)` | 查询取消通知 |

#### 类_FBrowser_服务器事件（嵌入式HTTP/WS服务器）

| 方法 | 说明 |
|------|------|
| `服务器即将创建(服务器)` / `服务器即将销毁(服务器)` | 生命周期 |
| `收到客户端连接(服务器, 连接ID)` / `收到客户端断开连接(服务器, 连接ID)` | 连接管理 |
| `收到HTTP请求(服务器, 连接ID, 客户端地址, 请求)` | ⭐HTTP请求处理；`服务器.发送Http响应(连接ID, 状态码, 头, 数据)` |
| `收到WebSocket请求(服务器, 连接ID, 客户端地址, 请求, 回调)` | WS连接请求；回调.继续接受或取消 |
| `收到WebSocket连接(服务器, 连接ID)` | WS连接建立 |
| `收到WebSocket消息(服务器, 连接ID, 数据指针, 数据长度)` | ⭐WS消息；`服务器.发送WebSocket数据(连接ID, 数据指针, 数据长度)` |

#### 类_FBrowser_资源处理器（自定义资源处理）

| 方法 | 说明 |
|------|------|
| `打开(标识ID, 请求, 处理请求可设, 回调)` → 逻辑型 | 决定是否处理此请求；处理请求设真+返回真=立即处理，设假+回调=异步 |
| `处理请求(标识ID, 请求, 回调)` → 逻辑型 | "打开"中设置异步时触发，调用回调.继续()开始 |
| `取响应头(标识ID, 响应, 响应长度可设, 重定向地址可设)` | 设置MIME/响应长度；重定向地址与响应长度互斥 |
| `忽略(标识ID, 可忽略大小, 被忽略大小可设, 资源忽略回调)` → 逻辑型 | 设置需要忽略的数据大小（待验证触发条件） |
| `读取(标识ID, 输出数据指针, 读取大小, 读入大小可设, 资源读取回调)` → 逻辑型 | ⭐写入替换数据到缓冲区；返回真继续，返回假停止；数据不能超过读取大小需分包 |
| `退出(标识ID)` | 取消/结束请求 |

#### 类_FBrowser_资源过滤器（修改响应体）

| 方法 | 说明 |
|------|------|
| `初始化过滤器(标识ID)` → 逻辑型 | 返回假取消拦截 |
| `获取数据(标识ID, 数据指针, 数据大小)` | 获取原始响应数据片段（UTF8） |
| `修改数据(标识ID, 输入指针, 输入大小, 输入读取大小可设, 输出指针, 输出大小, 输出写入大小可设)` → 整数 | ⭐修改响应数据；返回`资源过滤器.需要更多数据/完成/错误`；数据不能超过输出大小需分包 |
| `结束(标识ID)` → 逻辑型 | 过滤完成；返回真释放自身（动态创建时） |

#### 类_FBrowser_开发者消息事件（VIP DevTools协议）

| 方法 | 说明 |
|------|------|
| `开发者消息_VIP_收到消息` | DevTools 协议原始消息 |
| `开发者消息_VIP_执行完成` | 方法调用结果 |
| `开发者消息_VIP_收到事件` | DevTools 事件通知 |
| `开发者消息_VIP_已附加/已分离` | Agent 连接/断开 |

#### 类_FBrowser_URL请求事件（独立URL请求）

| 方法 | 说明 |
|------|------|
| `开始创建(标识, URL请求)` | 请求创建开始 |
| `获取到数据(标识, URL请求, 数据)` | 收到响应数据块 |
| `读取结束(标识)` | 所有数据接收完毕 |
| `即将完成` / `上传进度` / `下载进度` | 请求生命周期 |

**补充事项**：
- `扩展插件_创建成功/失败/载入成功/卸载成功` — CEF插件生命周期事件（应用事件中）
- `进程间消息_收到主进程消息` — 渲染进程收IPC（应用事件）
- `进程间消息_收到渲染进程消息` — 主进程收IPC（浏览器事件，即`浏览器_收到消息`）
- `浏览器_即将改变媒体访问` — 注意参数为 `(浏览器, 包含视频访问, 包含音频访问)`，非框架+许可类型

---

### FBroConst

**包**：`FBrowser.常量`

| 类 | 关键常量 |
|----|---------|
| `状态` | `默认=0`, `启用=1`, `禁用=2` |
| `日志级别` | `默认...致命...禁用(99)` |
| `按键事件` | `单击=0`, `按下=1`, `放开=2`, `字符型=3` |
| `鼠标按键类型` | `左键=0`, `中键=1`, `右键=2` |
| `值类型` | `无效=0`, `空=1`, `逻辑=2`, `整数型=3`, `双精度小数型=4`, `文本=5`, `字节集型=6`, `字典值型=7`, `列表型=8` |
| `资源类型` | `主框架=0`, `子框架=1`, `样式表=2`, `脚本=3`, `图片=4`, `XHR=13` 等18种 |
| `文件对话框` | `打开=0`, `打开_多=1`, `打开_文件夹=2`, `保存=3` |
| `JS对话框类型` | `警告=0`, `确认=1`, `提示=2` |
| `清理缓存` | `Cookies=2`, `LocalStorage=16`, `全部=4294967295` |
| `JS返回值类型` | `无效=-1`, `空值=0`, `逻辑值=2`, `整数值=3`, `双精度小数值=4`, `文本值=5` |
| `链接打开方式` | `当前选项卡=1`, `新前台选项卡=3`, `新弹窗=5`, `新窗口=6` |
| `VIP过滤器地址` | `完全匹配=0`, `模糊匹配=1`, `模糊头=2`, `模糊尾=3`, `正则匹配=4` |
| `DPI模式` | `无感知=-1`, `系统感知=-2`, `按显示器感知=-3`, `按显示器感知V2=-4` |
| `TLS版本` | `TLS_1=769`, `TLS_1_1=790`, `TLS_1_2=791`, `TLS_1_3=792` |
| `进程类型` | `主进程=0`, `渲染进程=1`, `GPU进程=2` 等（取当前进程类型用） |
| `进程` | `浏览器`, `渲染` — 进程间消息使用 |
| `线程ID` | `UI` — 任务投递目标线程 |
| `窗口运行风格` | `默认=0`, `谷歌=1`, `经典=2` |
| `窗口打开配置` | `当前选项卡=1`, `新前台选项卡=3`, `新弹窗=5`, `新窗口=6` |
| `事件标识` | `无`, `ctrl键`, `shift键`, `alt键` 等 — 按键/鼠标修饰位 |
| `事件数据类型` | 所有事件类型的枚举值（用于智能指针类型检查） |
| `请求标识` | `跳过缓存=1`, `禁用缓存=4`, `允许存储凭证=8` 等 |
| `URL请求状态` | `成功=0`, `读错误=1`, `取消=2`, `下载错误=3` |
| `过渡类型` | `链接=0`, `提交=1`, `刷新=2` 等 |
| `Cookie同站` / `Cookie优先级` | `无=0`, `宽松=1`, `严格=2` / `低/中/高` |
| `拖动操作` | `无=0`, `复制=1`, `链接=2`, `移动=16`（可位或） |
| `JSON解析选项` | `默认=0`, `宽松=1` |
| `VIP授权类型` / `VIP授权功能` | 授权相关信息 |
| `来路策略` | `默认` — 设置请求Referrer的策略 |
| `V8性能属性` | `无` — V8值属性设置标志 |
| `资源过滤器` | `需要更多数据`, `完成`, `错误` — 过滤器修改数据的返回状态码 |
| `焦点` | 焦点来源类型常量（`浏览器_请求焦点`事件使用） |
| `触摸事件` | `释放=0`, `按压=1`, `移动=2`, `取消=3` |
| `SEEK` | `SET=0`, `CUR=1`, `END=2` — 流读取位置参照 |
| `V8访问控制` | `默认=0`, `可读=1`, `可写=2`, `禁止覆盖=4` |
| `V8性能属性` | `无=0`, `只读=1`, `不能枚举=2`, `不能删除=4` |
| `节点类型` | `无支持=0`, `元素=1`, `属性=2`, `文本=3`, `CDATA=4`, `处理指令=5`, `注释=6`, `文档=7`, `文档类型=8`, `文档碎片=9` |
| `节点文档类型` | `未知=0`, `HTML=1`, `XHTML=2`, `插件=3` |
| `文本输入类型` | `默认~搜索` — 虚拟键盘输入模式（CEF_TEXT_INPUT_MODE_*） |
| `绘制元素类型` | `视图=0`, `弹窗=1` — 离屏渲染绘制类型 |
| `URI保留规则` | `无/正常/空格/路径分隔符/Url特殊字符/空格代替加号` — URI编解码选项 |
| `颜色类型` | `RGBA=0`, `BGRA=1` — 图片颜色格式 |
| `透明类型` | `OPAQUE=0`, `PREMULTIPLIED=1`, `POSTMULTIPLIED=2` |
| `PDF打印_边框` | `默认=0`, `无=1`, `最小=2`, `CUSTOM=3` |
| `进程异常` | `非0退出=0`, `被终止=1`, `段错误=2`, `内存超限=3` — 渲染进程终止状态 |
| `屏幕方向类型` | `默认=0`, `竖屏16_9=1`, `竖屏4_3=2`, `横屏16_9=3`, `横屏4_3=4` |
| `窗口风格` | `风格=@GWL_STYLE`, `扩展风格=@GWL_EXSTYLE` |
| `许可反馈` | `通过=0`, `否决=1`, `驳回=2`, `忽略=3` — 权限提示结果 |
| `菜单颜色类型` | `文本/文本_激活/文本_快捷键/文本_快捷键_激活/背景/背景_激活/计数` |
| `快捷菜单_编辑类型` | `无/可省略/可剪切/可复制/可粘贴` |
| `快捷菜单_事件标识` | `无/CAPS_LOCK/SHIFT/CONTROL/ALT/鼠标左键~中键~右键` |
| `VIP过滤器` | `修改匹配数据=0`, `修改全部匹配数据=1`, `插入匹配数据=2`, `插入全部匹配数据=3` |

---

### FBroDataType

**包**：`FBrowser.数据类型`

**配置结构体**：
- `FBrowser_初始化配置` — `无沙箱`, `执行进程`, `缓存目录`, `保留Session`, `日志目录`, `日志模式`, `本地化语言`, `接收语言清单`, `启用系统消息循环`, `启用离屏渲染`, `启用外部消息`, `启用进程间消息`, `远程端口` 等30+字段
- `FBrowser_窗口信息` — `额外风格`, `窗口名`, `风格`, `横坐标`, `纵坐标`, `宽度`, `高度`, `父窗口句柄`, `启用离屏渲染`, `运行风格`
- `FBrowser_浏览器配置` — `离屏渲染帧率`, `标准字体类型`, `默认字体大小`, `启用JS`, `启用图片加载`, `启用WebGL`, `背景颜色`, `窗口less帧率` 等
- `FBrowser_请求环境配置` — `缓存文件夹名`（为空=全局共享）, `保留session`, `接收语言清单`, `忽略证书错误`
- `FBrowser_禁用事件` — 18个 `禁用XXX事件` 布尔字段，可屏蔽特定事件回调

**数据结构体**：
- `FBrowser_弹出功能` — 弹窗属性描述
- `FBrowser_位置` — `X`(整数), `Y`(整数)
- `FBrowser_尺寸` — 宽度, 高度
- `FBrowser_矩形位置` — 横/纵/宽/高
- `FBrowser_按键事件` — `类型`(单击=0/按下=1/放开=2/字符型=3), `修饰位`(事件标识.*), `系统按键`, `字符值`
- `FBrowser_鼠标事件` — `横坐标`, `纵坐标`, `修饰键`
- `FBrowser_系统事件` — 系统级事件信息
- `FBrowser_触摸事件` — `ID`, `类型`(按压等), `横坐标`, `纵坐标`, `横坐标半径`, `纵坐标半径`, `旋转角度`, `压力`, `修饰键`
- `FBrowser_Cookie数据` — `名`, `值`, `域`, `路径`, `是否只有http`, `是否有过期时间`, `过期时间`(FBrowser_时间类型), `访问时间`(FBrowser_时间类型), `同站`, `优先级`
- `FBrowser_时间类型` — `年`, `月`, `日`, `时`, `分`, `秒`
- `FBrowser_屏幕信息` — 屏幕DPI/比例等
- `FBrowser_光标信息` — 自定义光标数据
- `FBrowser_音频参数` — `通道布局`, `采样频率`, `帧缓冲`
- `FBrowser_截图边框数据` — `横坐标/纵坐标/宽度/高度/缩放`
- `FBrowser_PDF打印设置` — 15个字段：`横向模式/打印背景/缩放比例/页面宽度~高度/首选CSS页面大小/边框类型~顶点~右边~底部~左边/页面范围/显示页眉页脚/头模板~页脚模板/生成带标签的PDF~文档大纲`
- `FBrowser_加速绘制额外信息` — 加速绘制元数据（timestamp, coded_size等）
- `FBrowser_加速绘制信息` — `共享纹理句柄`, `颜色类型`, `额外信息`
- `FBrowser_范围` — `来源`, `目标`（用于IME操作和文本选择）
- `FBrowser_下划线组成` / `FBrowser_下划线组成数组` — IME组合文本下划线数据
- `FBrowser_预查找数据` — `searchId`, `resultCount`
- `FBrowser_VIP触摸事件` — `横坐标/纵坐标/横半径/纵半径/旋转角度/压力/id`
- `FBrowser_媒体硬件数组` — `加入数据/删除数据/取驱动ID/取驱动名/取分组ID/清空全部数据/取数量`

**数组容器**：
- `FBrowser_文本数组` — `创建()`, `取个数()`, `加入成员(文本)`, `取数据(索引)`→文本型, `清空()`, `到火山文本数组()`→文本数组类
- `FBrowser_双文本数组` — key-value列表; `取个数()`, `到数组首()`, `取当前位置数据()`→FBrowser_双文本, `到下一个()`, `查找数据(key)`→整数
- `FBrowser_双文本` — `name`(文本型), `value`(文本型)
- `FBrowser_文本` — 可修改文本值（用于参数传递回写）
- `FBrowser_拖拽位置数组` — 离屏渲染拖拽区域列表

---

### FBroValue

**包**：`FBrowser.值`

| 类 | 说明 | 关键方法 |
|----|------|---------|
| `类_FBrowser_值` | CefValue | `创建()`, `是否有效()`, `取类型()`→值类型, `取/置逻辑(值)/整数/小数/文本/字节集/字典/列表(值)`, `复制()` |
| `类_FBrowser_列表值` | CefListValue | `创建()`, `是否有效()`, `是否只读()`, `置大小(数量)`, `取大小()`, `取/置整数值/文本值/逻辑值/小数值/双精度小数值/字典值/列表值(索引, 值)`, `移除(索引)`, `清空()`, `取文本值(索引)` |
| `类_FBrowser_字典值` | CefDictionaryValue | `创建()`, `是否有效()`, `取大小()`, `取/置文本值/整数值/逻辑值/小数值(关键字, 值)`, `是否存在关键字(关键字)`→逻辑型, `取关键字()`→FBrowser_文本数组, `移除(关键字)`, `清空()` |
| `类_FBrowser_V8值` | V8值 | **静态**（全部）: `创建未定义/空/逻辑值/整型值/无符号整型值/双精度小数值/日期值(时间类型)/文本值/类(存取器,拦截器,标识)/数组值(长度)/数组缓存值(指针,长度)/函数(名,处理程序,标识)` | **实例**: 全部类型判断（`是否有效/未定义/空/逻辑/整数/无符号整数/双精度小数/日期/文本/类/数组/数组缓存/函数`）, `取/置值_关键字/索引`, `取文本/整数/逻辑/双精度小数/时间值`, `执行函数/指定环境执行函数`, `取函数执行事件/函数名/关键字`, `是否存在/删除值_关键字/索引`, `是否有异常/取异常/清理异常/将重新抛出异常/置重新抛出异常`, `取数组元素数量/清空数组缓存/取外部分配的内存/调整外部内存大小`（30+方法） |
| `类_FBrowser_V8值数组` | V8值列表 | `创建()`, `取个数()`, `加入成员(V8值)`, `取数据(索引)`→类_FBrowser_V8值 |
| `类_FBrowser_值转换` | 全局静态转换 | `FBrowser_数据到字节集(指针,大小)`→字节集, `FBrowser_字节集到字节集(字节集)`→字节集, `FBrowser_文本到字节集(文本)`→字节集 |
| `类_FBrowser_V8环境` | V8执行环境 | `FBrowser_V8环境_取当前环境()`→V8环境, `FBrowser_V8环境_取运行环境()`→V8环境, `是否为空()`, `是否有效()`, `是否当前环境()`, `取任务处理器()`→任务运行器, `取浏览器()`→浏览器, `取框架()`→框架, `取全局V8值()`→类_FBrowser_V8值, `执行JS代码(代码, 错误链接, 行, 返回值, 异常)`, `进入()`, `退出()`, `是否相同(对比值)` |
| `类_FBrowser_V8异常` | V8异常信息 | `取消息()`→文本型, `取源代码行()`→文本型, `取脚本资源名()`→文本型, `取发生的行号()`→整数, `取开始/结束脚本索引()`→整数, `取开始/结束行()`→整数 |
| `类_FBrowser_V8堆栈踪迹` | V8调用栈 | `是否有效()`, `取框架数()`→整数, `取堆栈框架()`→类_FBrowser_V8堆栈框架 |
| `类_FBrowser_V8堆栈框架` | V8堆栈帧 | `是否有效()`, `取脚本名()`→文本型, `取脚本名或资源地址()`→文本型, `取函数名()`→文本型, `取行号()`→整数, `取列号()`→整数, `是否为执行JS代码()`→逻辑型, `是否构造()`→逻辑型 |

---

### FBroVip

**包**：`浏览器.VIP`

#### VIP注册（全局静态）

| 方法 | 说明 |
|------|------|
| `VIP注册_置授权码(授权码)` | 初始化前设置 |
| `VIP注册_取机器码()` → 文本型 | 本机机器码 |
| `VIP注册_取到期时间()` / `取开始时间()` / `取注册功能()` | 授权信息 |

#### 类_FBrowserVIP_控制器（通过 `浏览器.取VIP控制器()` 获取）

**指纹虚拟**（大量方法，核心列表）：
- `指纹_虚拟UserAgent(UA数据)`, `指纹_虚拟Languages/AppName/Platform/Product/Vendor`
- `指纹_虚拟Webglvendor/Webglrenderer`, `指纹_虚拟HardwareConcurrency`, `指纹_虚拟DeviceMemory`
- `指纹_虚拟Canvas_随机(最少, 最大, 种子)`, `指纹_虚拟WebGL_随机(...)`, `指纹_虚拟Audio_随机(...)`
- `指纹_虚拟Plugins(类型, JSON)`, `指纹_虚拟屏幕XY/分辨率/像素比`
- `指纹_虚拟WebrtcIP(公网IP, 本地IP, host, 禁用)`, `指纹_虚拟Date时区(时, 分, 时区名)`
- `指纹_虚拟定位(经度, 纬度, 精确度...)`, `指纹_虚拟屏幕方向(类型, 角度)`
- `指纹_设置SSL加密套件(TLS最小, 最大, 加密文本)`, `指纹_启用触摸事件(启用, 点数)`
- `指纹_清空调用计数()`/`取调用计数()`

**高级操作**：
- `高级_设置代理(地址, 账号, 密码, 关闭S5提示)` — S5代理
- `高级_网页截图(格式, 质量, 边框, 表面, 视窗外, 回调)` — CDP截图
- `高级_执行JS(代码, 环境ID, ..., 回调)` — 指定环境执行
- `高级触摸_按下/放开/移动/单击(x, y)` — CDP触摸
- `高级鼠标_按下/放开/移动/单击/滚轮滚动` — CDP鼠标
- `高级键盘_按下/放开/单击/输入字符/输入文本` — CDP键盘
- `高级_创建标签浏览器(地址, 序号, 激活, 额外信息, 浏览器事件, 禁用事件, 标识)` — 谷歌模式下创建新标签
- `高级_安装CRX插件包(CRX路径)` — 在请求环境实例上调用，安装扩展
- `高级_载入插件路径(目录路径)` — 加载已解压的扩展插件
- `是否为空()` → 逻辑型 — VIP控制器是否可用

**WebSocket拦截**：`WebSocket_启用拦截()`
**内核开关**：`内核开关_禁用Debugger(禁用)`, `内核开关_设置CSS/Web/V8内核(版本)`
**开发者消息**：`开发者消息_发送消息(JSON)`, `开发者消息_执行方法(ID, 方法, 参数)`

#### 类_FBrowserVIP_WebSocket客户端（VIP模块）
- `连接(URL)`, `发送文本/数据`, `关闭`
- **事件**：通过回调接收消息（和VIP_通用回调配合使用）

#### 类_FBrowserVIP_开发者DOM

| 方法 | 说明 |
|------|------|
| `启用/禁用` | DOM agent开关 |
| `枚举DOM(深度, 穿透, 回调)` | 获取Document节点树 |
| `取节点属性/取节点源码(节点ID)` | 节点查询 |
| `置节点属性文本/置节点源码/移除节点(节点ID)` | 节点修改 |
| `预查找文本/取查找文本/清除查找` | CDP文本搜索 |

---

### FBroHelp

**包**：`FBrowser.辅助`

| 类 | 方法 | 说明 |
|----|------|------|
| `类_FBrowser_设置资源处理器` | `设置(资源处理器事件)→长整数` | 在`浏览器_获取资源处理器`事件中使用; 返回标识ID |
| `类_FBrowser_设置资源过滤器` | `设置(资源过滤器事件)` | 在`浏览器_获取资源过滤器`事件中使用 |
| `类_FBrowser_设置V8值` | `设置(V8值)` | V8处理程序/存取器/拦截器中设置返回值 |

#### 其他辅助类

| 类 | 关键方法 | 说明 |
|----|---------|------|
| `类_FBrowser_命令行` | 静态: `FBrowser_命令行_创建()`→命令, `FBrowser_命令行_取全局()`→命令; 实例: `是否为空()`, `是否有效()`, `是否只读()`, `取字符串()`→文本型, `取程序()`→文本型, `置程序(名)`, `是否存在项/某项(名)`, `取项值(名)`→文本型, `置值(值)`, `置项值(名,值)`, `是否存在额外参数()`, `取额外参数/置额外参数(文本)`, `插入值(值)`, `启用单进程模式/摄像头/跨框架操作模式/录音/自动播放/无头模式`, `禁用GPU/GPU缓存`, `忽略GPU禁用清单`, `设置远程调试端口(端口)`, `禁用代理`, `设置全局代理(地址,账号,密码)`, `VIP_高级_设置全局代理(地址,账号,密码,关闭S5提示)` | 在`即将处理命令行`事件中修改CEF启动参数 |
| `类_FBrowser_用户额外配置` | `置事件(事件智能指针, 禁用事件)`, `置标识(文本)`, `取标识()`→文本型, `取额外信息()`→类_FBrowser_字典值 | 在`浏览器_即将打开新窗口`等事件中控制新窗口行为 |
| `类_FBrowser_事件智能指针` | `创建(类)`, `引用(对象指针, 类)`, `取执行类(类)`→指定类类型, `是否为空()` | ⭐所有FBrowser事件的统一封装传递方式 |
| `类_FBrowser_图片` | `是否为空()`, `取像素宽度/高度()`→整数, `取图片信息(缩放比例, 小数类宽, 小数类高, 整数类宽, 整数类高)`, `取图片数据_PNG(比例, x, 宽, 高)`→类_FBrowser_字节集, `取字节集()`→字节集类 | 下载图片回调返回的图片对象 |
| `类_FBrowser_字节集` | `取字节集()`→字节集类, `取字节集指针()`→变整数, `取字节集长度()`→整数, `清空字节集()` | 框架自定义字节集 |
| `类_FBrowser_下载` | `是否仍在下载中/已下载完成/已取消()`, `取地址/原始地址/推荐文件名/内容描述/MIME类型/存储位置()`, `取开始时间/结束时间()`, `取已下载长度/总长度()`, `取下载百分比()`, `取现行下载速度()`, `取关联标识符()` | 下载状态查询（CefDownloadItem） |
| `类_FBrowser_菜单环境` | `取类型()`→整数(参考:菜单类型.*), `取横向/纵向位置()`, `取地址/完整地址/源地址/页地址()`, `取框架页地址/框架网页编码()`, `取选择文本/错误单词()`, `取字典建议()`→FBrowser_文本数组, `取媒体类型/媒体类型标识()`, `取编辑框类型标识()`, `是否存在图片()`, `是否为编辑框/选择框/Custom菜单()` | 右键菜单环境信息（CefContextMenuParams） |
| `类_FBrowser_菜单模式` | `清空菜单()`, `取数量()`, `添加菜单(ID,名称)`→逻辑型, `添加Check菜单/Radio菜单(名称,群ID)`, `添加分隔栏()`, `添加子菜单(ID,名称)`→类_FBrowser_菜单模式, `删除菜单(ID)`, `取/置菜单标签(ID,名)`, `取菜单类型/分组ID(ID)`, `取子菜单(ID)`→菜单模式, `是否可见/禁止/选中(ID)`, `置可见/禁止/选中状态(ID,值)`, `存在快捷键/快捷键_索引(ID)`, `设置快捷键(ID,键码,shift,ctrl,alt)`, `设置快捷键_索引`, `移除快捷键/快捷键_索引(ID)`, `取颜色(ID,颜色类型,整数类A/R/G/B)`, `选中状态_索引` | 右键菜单构造（CefMenuModel，20+方法） |
| `类_FBrowser_方案注册` | `添加自定义方案(方案名, 标志)` | 在`注册自定义方案`事件中注册scheme |
| `类_FBrowser_SLL信息` | `取状态()` | SSL证书信息 |

#### 填表框架（类_FBrowser_填表框架）

| 方法 | 说明 |
|------|------|
| `是否有效()`→逻辑型 | 填表框架是否可用 |
| `点击元素(CSS选择器, 索引)` | 模拟点击 |
| `滚动到元素(CSS选择器, 索引, 是否顶端对齐)` | 滚动到可见区域 |
| `置元素焦点(CSS选择器, 索引, 是否焦点)` | 设置焦点 |
| `置元素内容/取元素内容(CSS选择器, 索引, 内容/JS回调)` | 设置/获取value |
| `置元素选择框/取元素选择框(CSS选择器, 索引, 选择/JS回调)` | 设置/获取checkbox/radio |
| `置元素选择项/取元素选择项(CSS选择器, 索引, 选择项/JS回调)` | 设置/获取select选项 |
| `置元素内文本/取元素内文本(CSS选择器, 索引, 文本/JS回调)` | 设置/获取innerText |
| `置元素外文本/取元素外文本(CSS选择器, 索引, 文本/JS回调)` | 设置/获取outerText |
| `置元素内代码/取元素内代码(CSS选择器, 索引, HTML/JS回调)` | 设置/获取innerHTML |
| `置元素外代码/取元素外代码(CSS选择器, 索引, HTML/JS回调)` | 设置/获取outerHTML |
| `置元素属性/取元素属性(CSS选择器, 索引, 元素名, 值/JS回调)` | 设置/获取属性 |
| `元素是否存在(CSS选择器, 索引, JS回调)` | 异步判断元素存在 |
| `取元素坐标(CSS选择器, 索引, JS回调)` | 异步获取坐标 |
| `触发元素事件(CSS选择器, 索引, 事件名, 按键代码)` | 手动触发事件（如keydown等） |

#### 进程间通信类

| 类 | 关键方法 | 说明 |
|----|---------|------|
| `类_FBrowser_进程消息` | `创建(名称)`, `取名称()`→文本型, `取列表值()`→类_FBrowser_列表值 | 进程间IPC消息封装 |
| `类_FBrowser_POST元素` | `创建()`, `置数据_文本(文本)`, `取文本数据()`→文本型 | POST表单元素 |
| `类_FBrowser_POST数据` | `创建()`, `增加元素(POST元素)`, `取元素个数()`, `取元素()`→类_FBrowser_POST元素数组 | POST请求体 |
| `类_FBrowser_POST元素数组` | `取个数()`, `取当前位置数据()`→类_FBrowser_POST元素, `到数组首()`, `到下一个()` | POST元素列表遍历 |
| `类_FBrowser_URL请求` | `取请求状态码()`, `取请求()`→类_FBrowser_请求 | URL请求状态查询 |
| `类_FBrowser_拖拽数据` | `是否为空/只读/链接/碎片/文件()`, `取链接地址/标题/关联数据/碎片文本/源码/基础地址/文件名()`, `取图片/图片坐标()`, `是否有图片()`, `置链接地址/标题/关联数据/碎片文本/源码/基础地址()`, `增加文件(路径,显示名)`, `复制()` | CefDragData 拖拽操作数据 |
| `类_FBrowser_共享内存数据` | `是否为空()`, `取数据大小()`, `取数据()`→字节集类 | CefSharedMemoryRegion 共享内存 |
| `类_FBrowser_读取流` | | CefStreamReader 数据流读取 |

---

## 二、火山核心库 w_\*

### w_base, w_string, w_bin

**包**：`火山.基本`

#### 对象类（w_base）

| 方法 | 说明 |
|------|------|
| `对象类.标记值` → 变整数 | 用户自定义tag（读写属性） |
| `对象类.为指定对象类(对象, 类型)` → 逻辑型 | 类型检查（静态） |
| `对象类.新建对象(类类型)` → 对象 | 创建实例（静态） |
| `对象类.取对象自身指针(对象)` → 变整数 | |

**基础包装类**（用于传引用）：`字节类`, `短整数类`, `字符类`, `整数类`, `变整数类`, `长整数类`, `小数类`, `逻辑型类` — 每个有public `.值` 字段

#### 文本类（w_string）——所有方法是静态，第一参数为文本

**创建/转换**：
- `到文本(数据)` → 文本型 | 基本类型转文本
- `取格式文本(格式, 替换数据...)` → 文本型 | sprintf风格
- `文本到UTF8(文本, 结束零=真)` → 字节集类
- `UTF8到文本(字节集)` → 文本型
- `文本到多字节(文本)` / `多字节到文本(字节集)`
- `文本到整数(文本)` → 整数 | **NOT** `到整数`
- `文本到小数(文本)` → 小数 | **NOT** `到数值`
- `文本到逻辑值(文本)` → 逻辑型

**查询**：
- `取文本长度(文本)` → 整数
- `文本是否为空(文本)` → 逻辑型
- `寻找文本(被搜索, 搜索词, 起始=0, 区分=真)` → 整数 | 找不到返回-1, **1-based索引**
- `倒找文本(被搜索, 搜索词, 起始=0, 区分=真)` → 整数
- `是否以文本开头(文本, 前缀, 区分)` / `是否以文本结束(文本, 后缀)`
- `文本比较(文本1, 文本2, 区分=真)` → 整数 | -1/0/1

**截取**：
- `取文本左边(文本, 数目)`, `取文本右边(文本, 数目)`, `取文本中间(文本, 起始, 数目)` | **1-based起始**

**修改**：
- `加入文本(文本, 要加的)`, `插入文本(文本, 索引, 要插的)`
- `子文本替换(文本, 旧, 新, 起始=0, 次数=-1, 区分=真)`
- `替换所有子文本(文本, 旧, 新)`
- `删首尾空(文本)` / `删全部空(文本)`
- `到大写(文本)` / `到小写(文本)`

**分割**：
- `分割文本(文本, 分隔符集合, 结果数组, 删首尾空=真, 忽略空白=真)`
- `分割子文本(文本, 子文本, 结果数组)`

#### 字节集类（w_bin）

**创建**：`创建字节集(字节值...)`, `组合字节集(数据...)`, `从文件读字节集(字节集, 文件名)`, `到字节集(数据)`
**查询**：`取字节集长度(字节集)`, `寻找字节集(字节集, 查找内容, 起始)`, `取字节集数据(字节集, 偏移, 类型)`
**修改**：`添加字节集(字节集, 数据)`, `插入字节集(字节集, 偏移, 数据)`, `字节集替换(...)`, `子字节集替换(...)`
**转换**：`字节集到十六进制文本(字节集)`, `十六进制文本到字节集(文本)`, `字节集到BASE64文本(字节集)`, `BASE64文本到字节集(文本)`

---

### w_array

**包**：`火山.基本`

**模板数组类**（`整数数组类`, `文本数组类`, `逻辑型数组类`, `小数数组类`, `字节数组类`）：
- `取成员数()` / `是否为空()`
- `取成员(索引)` / `置成员值(索引, 值)`
- `加入成员(值...)` — 返回索引
- `插入成员(索引, 值)`, `删除成员(索引, 数目=1)`, `删除所有成员()`
- `查找首成员(值)` → 整数 | -1表示未找到
- `排序(从小到大=真)`
- `枚举循环` → `取枚举值()` / `取枚举索引()`
- `压入成员(值)` / `弹出成员()` — 栈操作

**特殊数组**：`文本排序数组类`（自动排序）, `文本独有数组类`（唯一+自动排序）

---

### w_math

**包**：`火山.基本`

**算术运算类**（全局静态）：
- `取较小值(a, b)` / `取较大值(a, b)` / `剪切值(值, min, max)`
- `四舍五入(值, 位置=0)`, `取整(小数)`, `绝对取整(小数)`
- `求次方(底, 幂)`, `求平方根(值)`, `求正弦/余弦/正切`
- `取随机数(min=0, max=最大整数)` → 整数
- `取随机小数(min=0, max=1)` → 小数
- `置随机数种子(种子=0)`

**数值转换类**（全局静态）：
- `文本到整数(文本)` → 整数 | **NOT** `到整数`
- `文本到长整数(文本)` → 长整数
- `文本到小数(文本)` → 小数 | **NOT** `到数值`
- `文本到逻辑值(文本)` → 逻辑型
- `到文本(值)` → 文本型
- `取十六进制文本(整数, 宽度=0)` → 文本型
- `十六进制文本到整数(文本)` → 整数

**几何**：`矩形类`, `位置类`, `尺寸类`, `小数矩形类`

---

### w_misc

**包**：`火山.基本` | **杂项工具集**（375KB，最大核心库文件，⭐必读）

#### 时间操作类（全局静态）

| 方法 | 返回 | 说明 |
|------|------|------|
| `取现行时间()` | 小数 | 当前系统日期时间 |
| `置现行时间(时间)` | 逻辑型 | 设置系统日期时间 |
| `到时间(文本, 中文格式=真)` | 小数 | 文本→时间 |
| `时间到文本(时间, 转换部分, 中文格式=真)` | 文本型 | 时间→文本 |
| `时间到格式文本(时间, 格式)` | 文本型 | 按格式输出（%Y %m %d %H %M %S） |
| `取年份(时间)` / `取月份(时间)` / `取日(时间)` | 整数 | 时间分量提取 |
| `取小时(时间)` / `取分钟(时间)` / `取秒(时间)` | 整数 | |
| `取星期几(时间)` | 整数 | 1=周日...7=周六 |
| `指定时间(年, 月, 日, 时, 分, 秒)` | 小数 | 组合时间 |
| `增减时间(时间, 字段, 增加值)` | 小数 | 时间加减 |
| `取时间间隔(时间1, 时间2, 字段)` | 小数 | 时间差 |
| `取某月天数(年, 月)` | 整数 | |
| `取时间戳(时间)` | 整数 | UNIX秒时间戳 |
| `时间戳到时间(时间戳)` | 小数 | UNIX秒→时间 |
| `取毫秒时间戳(时间)` | 长整数 | UNIX毫秒时间戳 |
| `毫秒时间戳到时间(毫秒戳)` | 小数 | UNIX毫秒→时间 |
| `取日期(时间)` / `取时间(时间)` | 小数 | 提取日期/时间部分 |

#### 常用功能类（全局静态）⭐最常用

| 方法 | 返回 | 说明 |
|------|------|------|
| `取启动时间()` | 长整数 | 自系统启动以来的毫秒数（GetTickCount64） |
| `延时(毫秒)` | void | Sleep |
| `运行(命令行, 等待, 显示方式, 退出码, 标准输出, 标准错误, UTF8)` | 逻辑型 | 运行外部程序并捕获输出 |
| `打开指定网址(URL, 显示方式)` | void | 用默认浏览器打开URL |
| `取剪辑板文本()` | 文本型 | |
| `置剪辑板文本(文本)` | 逻辑型 | |
| `清除剪辑板()` | void | |
| `取屏幕宽度/高度()` | 整数 | 像素 |
| `取鼠标位置()` | 位置类 | 屏幕坐标 |
| `取鼠标水平位置/垂直位置()` | 整数 | |
| `显示鼠标指针(显示)` | 整数 | |
| `取CPU核心数()` | 整数 | |
| `取推荐工作线程数(最多)` | 整数 | |
| `鸣叫(声音类型)` | void | 系统提示音 |
| `任务栏是否可视()` | 逻辑型 | |
| `锁定输入(锁定)` | void | 锁定/解锁键盘鼠标 |
| `为64位程序()` | 逻辑型 | |
| `取系统电源状态()` | 系统电源状态 | |

**注册表操作**（常用功能类内）：`取文本注册项/取文本注册项2(全路径)` → 文本型, `写文本注册项/写文本注册项2(全路径, 值)` → 逻辑型, `删除注册项/删除注册项2(全路径)` → 逻辑型（`2`为64位版）
**信息框**：`全局信息框(内容, 标题, 按钮, 图标)` → 整数, `全局询问信息框/全局错误信息框/全局警告信息框`

#### 磁盘操作类（全局静态）

| 方法 | 返回 | 说明 |
|------|------|------|
| `文件是否存在(路径)` | 逻辑型 | ⭐ |
| `目录是否存在(路径)` | 逻辑型 | ⭐ |
| `取文件尺寸(路径)` | 长整数 | ⭐ |
| `取文件时间(路径)` | 小数 | |
| `读入文件(路径, 尺寸=-1)` | 字节集类 | ⭐ 读取整个文件 |
| `写到文件(路径, 字节集, 尺寸=-1)` | 逻辑型 | ⭐ 写入文件 |
| `创建目录(路径)` | 逻辑型 | |
| `删除目录(路径, 模式, 仅检测)` | 逻辑型 | |
| `复制文件(源, 目标, 覆盖)` / `移动文件(源, 目标)` / `删除文件(路径)` | 逻辑型 | |
| `文件更名(旧名, 新名)` | 逻辑型 | |
| `取当前目录()` / `改变目录(路径)` | 文本型/逻辑型 | |
| `取临时文件名(目录)` | 文本型 | |
| `取磁盘总空间/剩余空间(盘符)` | 长整数 | |
| `取文件名后缀/取文件名路径/取文件名无路径部分(文件名)` | 文本型 | |
| `改变文件名后缀(文件名, 新后缀)` | 文本型 | |
| `连接路径(路径1, 路径2, 结果)` | 文本型 | |
| `到绝对路径(相对, 结果)` / `到相对路径(绝对, 基准, 结果)` | 文本型 | |
| `浏览选中文件(文件名)` | void | 资源管理器定位文件 |

#### 程序调试类（全局静态）

| 方法 | 说明 |
|------|------|
| `调试输出(数据...)` | ⭐ 输出到IDE调试窗口 |
| `取当前语句位置()` → 文本型 | 当前文件+行号 |
| `调试输出当前语句位置()` | 输出当前位置 |
| `取调试文本(数据...)` → 文本型 | 获取对象调试文本 |
| `调试信息框(数据...)` | 弹出调试信息框 |
| `是否为调试版()` → 逻辑型 | |

#### 其他工具类

**按键操作类**：`取按键名称(键码)` → 文本型, `取按键字符(键码)` → 字符, `取锁定键状态(键码)` → 逻辑型, `取键按下状态(键码)` → 逻辑型
**按键码**（常量类）：全部Windows虚拟键码常量（`A键~Z键`, `F1~F12`, `回车键`, `ESC键`, `空格键`, `退格键`, `Shift/Ctrl/Alt/Win键`, `CapsLock/NumLock键` 等）
**系统热键类**：属性写 `热键(按键代码)` — 注册全局热键；事件 `热键被按下()` → 整数
**颜色类**：`取颜色值(R,G,B)` → 整数, `取颜色红色分量/绿色分量/蓝色分量(颜色)`, `颜色偏移(颜色, 偏移)`
**环境存取类**：`取运行目录()`, `取模块目录()`, `取执行文件名()`, `取命令行(数组)`, `读环境变量/写环境变量(名, 值)`
**控制台操作类**：`运行控制台程序(命令行, 标准输出, 错误输出, ...)`, `标准输出(内容...)` / `标准输出行(...)`, `标准输入(回显)` / `标准输入字符(回显)`
**流程控制类**：`如果真(条件)`, `计次循环(次数)` + `取循环索引()`, `循环判断首/尾(条件)`, `分支判断(值)` + `分支(值)` + `默认分支()`, `选择(条件, 真值, 假值)`—三元, `结束(退出码)`
**指针操作类**：`取变量地址(变量)` → 变整数, `内存复制/移动/清零/填充`, `分配内存(尺寸)` → 变整数 + `释放内存(指针)`
**通用哈希表模板类**<K,V>：`唯一性插入(键,值)`→逻辑型, `插入(键,值)`, `取值(键)`→V, `是否存在(键)`, `删除(键)`, `清空()`, `取成员数()`, `枚举循环()`+`取枚举关键字()`/`取枚举值()`. 预置: `整数到文本哈希表`, `文本到文本哈希表`, `文本到整数哈希表`
**GUID类**：`创建()`→逻辑型, `从文本创建("{...}")`, `取文本()`→文本型, `取字节集()`→字节集类
**文件查找类**：`寻找文件(通配符, 属性, 跳过属性)`→文本型(返回首个匹配) + 事件`找到文件`
**驱动通信类**：驱动级通信（高级）
**文件读写类**（实例）：`打开文件(路径, 方式, 共享)`, `关闭文件()`, `读入字节集(尺寸)`→字节集, `写出字节集(数据)`, `读入文本/读入一行(编码)`, `写出文本/写文本行(...)`, `移到文件首/尾()`, `取文件长度()`, `锁住/解锁文件(...)`

---

### w_stream

**包**：`火山.基本`

**输入流类**：`打开(流, 文件名)`, `读(流, 变量)`→流, `读数据(流, 尺寸, 字节集)`, `是否到尾部(流)`
**输出流类**：`打开(流, 文件名, 附加=假)`, `写(流, 数据)`→流, `写数据(流, 字节集)`, `刷新(流)`
**内存输入流**：`打开(流, 字节集)`, `打开2(流, 地址, 尺寸)`
**内存输出流**：`取数据(流)`→字节集
**错误**：所有流有 `错误码` 属性, `清除错误(流)`, `是否成功(流)`

---

### w_muti_thread

**包**：`火山.基本`

**同步原语**：
- `互斥锁类`：`加锁(仅尝试=假)`, `解锁()`
- `信号灯类`：`等待(尝试)`, `限时等待(毫秒)`, `放行()`
- `同步事件类`：`等待()`, `放行()`, `重置()`
- `读写锁类`：`开始读()/结束读()`, `开始写()/结束写()`

**线程**：
- `线程操作类.启动线程(函数, 参数, 栈, 优先, 等待)` — 静态
- `线程类`：`启动()`, `停止()`, `通知停止()`, `是否需要退出(等待)` + 定义事件 `线程运行`
- `全局线程池.启动线程(函数, p1, p2)` → 句柄
- `主线程处理器.发送通知(通知码, p1, p2)` / `投递通知(通知码, p1)`

**原子类型**：`原子整数类`, `原子长整数类`, `原子逻辑型类` — `取值()`, `置值()`, `替换值()`, `加(数)`, `比较赋值(比较, 新)`

---

### w_network

**包**：`火山.基本`

- `网络通信.通信测试(地址, 超时=10000)` → 整数 | Ping, 返回ms或-1
- `网络通信.转换为IP地址(主机名)` → 文本型 | DNS解析
- `客户`（TCP客户端）：`连接(IP, 端口)`, `断开连接()`, `发送数据(数据...)`, `是否已连接()` + 事件 `数据到达(数据)`
- `服务器`（TCP服务端）：`开始监听()`, `发送数据(地址, 超时, 数据)`, `断开客户(地址)`, `取客户数目()`
- `数据报`（UDP）：`初始化()`, `发送数据(主机, 端口, 数据)`, 多播支持 + 事件 `数据到达(数据, 对方地址, 端口)`

---

### w_sqlite3

**包**：`火山.视窗.SQLite数据库`

**SQLite数据库类**：
- `打开(文件名, 选项=-1)` | `:memory:` 为内存数据库
- `执行SQL语句(语句, 错误内容)` → 整数 | 返回结果码
- `取记录集(语句, 偏移=-1)` → SQLite记录集类 | 预编译语句
- `取最后插入行()`, `取影响行数()`
- `取表内容(语句, 字段数组, 内容数组, 错误)` | 快速查询辅助
- `关闭()` | 同析构
- `启用外键约束/触发器/防御功能`

**SQLite记录集类**：
- `执行语句()` → 整数 | SQLITE_ROW/SQLITE_DONE
- `重置()`, `释放()`
- `读整数数据(列索引)`, `读文本数据(列索引)`, `读小数数据(列索引)`, `读字节集数据(列索引)`
- `置文本参数数据(参索引, 值)`, `置整数参数数据(...)`, `置空参数数据(...)`

**常量**：`SQLite打开方式.*`, `SQLite执行结果.*`

---

### w_yyjson

**包**：`火山视窗.JSON支持` | 详见 [yyjson-api.md](yyjson-api.md)

**YYJSON只读对象类** — 只解析（性能最高）：
- `创建自文本(JSON文本)` → 逻辑型
- `取文本(键名)` → 文本型, `取整数(键名)` → 整数, `取小数(键名)` → 小数
- `取对象(键名)` → 只读对象类, `取数组(键名)` → 只读数组类
- `取路径文本(路径)` — JSON Pointer（以`/`开头）
- `是否为空/对象/数组/文本/整数/数值/逻辑型()` — 类型检查
- `到可读文本(选项=缩进)` → 文本型

**YYJSON对象类** — 构造+解析：
- `加入文本成员(键, 值)`, `加入整数成员(键, 值)`, `加入逻辑值成员(键, 值)`
- `加入成员(键, 对象)`, `加入数组成员(键, 数组)`
- `删除成员(键名)`, `清空()`, `取成员数()`
- `到可读文本(选项)` → 文本型 | **YYJSON格式化选项.压缩** = 无换行

**YYJSON数组类** — 构造数组：
- `加入成员(对象)`, `加入文本成员(值)`, `加入整数成员(值)`, `加入新成员()` → 返回空对象
- `取成员(索引)` → YYJSON对象类, `取成员数()` → 整数

---

### w_com

**包**：`火山.基本`

**COM对象类**：`创建(对象, COM类型, 类型库)`, `获取(对象, 类型)` — 获取运行实例, `是否为空()`, `清除()`, `读属性(名称, 返回类型, 参数...)`, `写属性(名称, 参数...)`, `方法(名称, 返回类型, 参数...)`, `无返回值方法(名称, 参数...)`, `是否成功()`, `取接口(IID, 结果)`
**COM变体型**：`取值/赋值(变体, 值, 索引)`, `取类型()`, `创建数组(类型, 数目)`
**COM初始化类**：实例化即自动 `CoInit/CoUninit`

---

### w_res

**包**：`火山.基本`

**位图对象类**：`载入位图资源(资源)`, `载入位图文件(文件名)`, `取图片尺寸()→尺寸类`, `取图片数据(字节集, 宽, 高)`, `写图片文件(文件名, ...)`
**字体对象类**：`创建字体(名称, 尺寸, 粗体, ...)`, `从描述文本创建字体(描述)`, `取文本尺寸(文本)→尺寸类`, `取默认GUI字体()`
**图标对象类**：`载入图标资源(资源)`, `载入图标文件(文件名)`
**鼠标指针对象类**：`取标准鼠标指针(类型)` | 常量: `标准箭头型`, `十字型`, `手型`, `沙漏型`

---

### w_startup

**包**：`火山.基本`

**程序类**：`取程序()` → 程序实例, `取全局对象(类类型)` → 全局单例, `投递退出消息(退出码)`, `取用户程序名称/版本号`, `取火山平台版本号()`
**窗口程序类**：`消息过滤(句柄, 消息, wParam, lParam)` — 可覆盖, `空闲(已空闲时间)` — 空闲处理
**动态库程序类**：`入口通知(模块句柄, 原因, 保留)`, `将被卸载()`

### w_exception

**包**：`火山.基本`

**异常管理**：`开始俘获异常`, `俘获异常(数据类型)`, `俘获所有异常`, `抛出异常(数据)`, `抛出火山异常(代码, 描述)`, `再次抛出异常`

### w_device

**包**：`火山.基本`

**串口类**：属性 `端口号, 波特率, 数据位数...`, 方法 `启动()`, `停止()`, `发送数据(数据...)`, 事件 `数据到达(字节值)`

### w_multi_media

**包**：`火山.基本`

**媒体播放类**（静态）：`播放WAV资源/文件/数据(数据, 循环, 同步)`, `停止播放Wav声音()`
**声音播放器**：`播放文件/数据/资源(数据, 循环)`, `停止/暂停/继续`, 属性 `音量`(0-1)

### w_service

**包**：`火山.基本`

**服务类**（静态）：`运行(服务处理类)`, `安装服务/卸载服务(名称)`, `启动/停止/暂停/继续服务(名称)`, `记录事件(来源, 消息, 类型)`

---

## 三、社区/第三方库

### 加密解密库

**包**：`火山.仰望`

**加解密类_**：
- `加密文本_(明文, 钥匙, 密钥长度=16/32, 分组模式=AES_ECB/CBC)` → 文本型 | AES加密
- `解密文本_(密文, 钥匙, 密钥长度=16/32, 分组模式)` → 文本型 | AES解密
- `加密字节集_(数据, 钥匙, 长度, 模式)` → 字节集类
- `取字节集数据摘要_(数据, Hash类型)` → 文本型 | MD5/SHA1/SHA256/SHA512
- `取文本数据摘要_(文本, Hash类型)` → 文本型
- `取文件数据摘要_(路径, Hash类型)` → 文本型

**MD5类_**：`取数据摘要_(字节集, 小写?)` → 文本型, `取数据摘要2_(文件路径)` → 文本型
**CRC校验类_**：`取数据摘要_CRC32(字节集, 初始值)` → 文本型
**BCC校验类_**：`BCC校验_取校验码(数据)` / `BCC校验_数据是否完整(数据)`
**XxHash类_**：`取数据摘要_XxHash_数据(数据, 长度, 种子)` → 文本型
**加解密解压缩类_**：LZ4压缩（CNG API）
**CNG加解密类** ⭐：Windows CNG API完整封装（AES/DES/3DES/RSA），支持多种链接模式(ECB/CBC/CFB/...)和填充方式(PKCS7/Zero/...)，算法类型含 `CNG算法类型.*`、`CNG链接模式.*`、`CNG数据填充方式.*`、`CNG密钥类型.*`、`CNG_RSA填充方式.*` 等常量类
**Access辅助类_**：Access数据库加解密

---

### 网络访问库

**包**：`火山.仰望`

**网址IP处理类_**：
- `域名转IP地址_(域名)` → 文本型
- `取网址域名_(URL)` → 文本型
- `取网址端口_(URL)` → 文本型

**代理IP类_**：`置代理IP_(IP:Port)` | 空=清除
**网络判断类_**：`判断网络状态_(信息文本)` → 文本型 | 返回 LAN/Dialup/Proxy/NoInternet
**SSL缓存类_**：`开启使用SSL及TLS_(64位?)`, `开启SSL缓存_(64位?)`

---

### 文件操作库

**包**：`火山.仰望`

**文件属性类_**：`取文件属性_(路径)` → 整数, `置文件属性_(路径, 属性值)`
**文本数据读取类_**：
- `加载变量(文本变量, 字节集变量, 限制行数, 是否为ANSI)`
- `读入文本行(行号)` → 文本型
- `删除文本行(行号)` / `修改文本行(行号, 新内容)` / `插入文本行(行号, 内容)`
- `取出修改数据()` → 文本型

---

### 解压缩操作库

**包**：`火山.仰望`

**数据解压缩类_**：
- `压缩数据_(数据, 等级)` → 字节集类 | zlib压缩
- `解压数据_(数据)` → 字节集类 | zlib解压

**数据解压缩类2_**：
- `压缩数据_Gzip/Zlib(数据)` → 字节集类 | HP-Socket
- `解压数据_Gzip/Zlib(数据)` → 字节集类

**ZIP压缩类_**：`打包_(路径)`, `解包_(路径)`, `添加(文件)`, `置密码_(密码)` + 进度事件
**ZIP解压类_**：基于HP-Socket的ZIP解压
**文件压缩类_** / **文件解压类_**：完整的压缩/解压类，支持元数据读取、进度回调
**压缩包读写类** ⭐：基于7-zip（bit7z），支持7z/zip/rar/tar/gz等格式
**压缩包编辑类**：编辑现有存档（重命名/更新/删除文件）
**Deflate解压**：`解压数据(字节集, 缓存大小)` → 字节集类

---

### 数据操作库

**包**：`火山.仰望`

**拼接符**（常量）：`回车符`(\\r\\n), `换行符`(\\n), `制表符`(\\t), `单引号`, `双引号`, `反斜杠`
**剪辑板类_**：`剪辑板_置剪辑板图片(路径)`, `剪辑板_置剪辑板图片2(数据)`, `剪辑板_置剪辑板文件(文件列表, 复制)`, `剪辑板_保存剪辑板文件(路径, 覆盖)`

---

### 编码进制库

**包**：`火山.仰望`

**SIMD编码转换类_**（SSE2/AVX2/NEON/AVX-512加速）：
- `UTF8到文本_(字节集, 校验?)` → 文本型 | ⭐常用
- `文本到UTF8_(文本, 结束零?, 校验?)` → 字节集类 | ⭐常用
- `多字节到UTF8_(字节集)` / `UTF8到多字节_(字节集)` | GBK/ANSI ↔ UTF-8
- `UTF8到UTF16LE_/UTF16BE_/UTF32_()` | UTF-8到其他编码
- `UTF16LE到UTF8_/UTF32_()`, `UTF16BE到UTF8_/UTF32_()`, `UTF32到UTF8_/文本_/UTF16LE_/UTF16BE_()`
- `验证UTF8_/验证ASCII_/验证UTF16LE_/验证UTF16BE_/验证UTF32_()` → 逻辑型

**编码转换类_**：非SIMD版编码转换（多字节↔文本等）
**URL编码类_**：URL编码/解码
**进制转换类_**：更广泛的进制转换
**编码判断类_**：检测文本编码（UTF-8/16/32/ANSI）
**Emoji表情处理类_**：Emoji编解码
**UCS2编码转换类_**：UCS-2编码转换

**任意进制转换类_**：
- `十进制转任意进制_(十进制, 目标进制)` → 文本型 | 支持2-36进制
- `任意进制转十进制_(文本, 源进制)` → 整数

---

### 数据匹配库

**包**：`火山.仰望`

**正则类_**（VBScript.RegExp）：
- `创建(正则文本, 匹配内容, 区分大小写, 多行, 全局)`
- `取匹配数量()` → 整数
- `取匹配文本(索引, 起始位置)` → 文本型
- `取子匹配文本(索引, 子索引, 起始位置)` → 文本型
- `替换(替换文本)` → 文本型

**数据匹配类_正则**：`是否为整数_(文本)`, `是否为小数_(文本)`, `匹配所有小数_(文本, 数组)`

---

### 数据库操作

**包**：`火山.仰望`

**数据库类_**（ADO）：
- `连接文本/连接超时/执行超时` — 读写属性
- `执行SQL语句(SQL, 选项, 受影响行数, 错误描述)` → 逻辑型
- `执行SQL语句2(SQL, 选项, 受影响行数)` → 记录集类_
- `开始事务/保存事务/回滚事务`
- `关闭()`, `取错误信息()`

---

### JSON操作库

**包**：`火山.仰望` | 详见 [yyjson-api.md](yyjson-api.md)

**JSON对象类_**（简化版，支持JSON Pointer路径）：
- `创建自文本(内容)` → 逻辑型
- `置入文本值/整数值/长整数值/小数值/逻辑值/对象值/数组值(键, 值, 查找=真)`
- `取文本值/整数值/长整数值/逻辑值/小数值(键)` | 键名支持 `/path/to/node`
- `成员是否存在(键)`, `删除成员(键)`, `取所有键名(数组)`, `取所有键值(数组)`
- `到可读文本(标志)` → 文本型

**JSON辅助类_**（全局静态）：`JSON_取所有键名(文本数据, 字节集数据, 键名组)` → 整数, `JSON_取所有键值(...)` → 整数

---

### 图像处理库

**包**：`火山.仰望`

**WEBP图片处理类_**：WebP编解码（基于libwebp），含嵌入式stb_image_write用于BMP/TGA

### OCR识别库

**包**：`火山.仰望`

**RapidOCR识别类_**（CPU版）：
- `加载模型_(线程数, 检测模型, 语言模型, 字典)`
- `识别_多行(模型, 图片, 参数...)` → 文本型 | 含位置和置信度
- `识别_单行(模型, 图片)` → 文本型
- `识别_找字(模型, 图片, 关键词)` → OCR结果类

**RapidOCR识别类_GPU_**：同上，NVIDIA CUDA 11.8+加速

**验证码识别类DdddOcr_X64/X86**：`初始化_(线程数)`, `识别图片_(图片数据)` → 文本型

**模型常量**：`OCR检测模型.中文_OCRv3/v4/英文/多语言`, `OCR语言模型.*`（13种语言）

---

### 条码二维码库

**包**：`火山.仰望`

**条码解析类_**（ZBar）：`解析图片(图片数据, 解码信息, 解码结果数组)` → 逻辑型
**二维码类_**（qrencode）：`生成二维码_(内容, 缩放, 版本, 容错, RGB, Logo, 圆化Logo...)`, `保存二维码_(路径)`, `取二维码尺寸_()`

---

### 脚本操作库

**包**：`火山.仰望`

**JS脚本类_**（ChakraCore）：
- `初始化_()`, `清除_()`
- `Eval_(表达式, URL)` → 结果指针
- `AddCode_(代码, URL)` → 逻辑型
- `运行函数_(表达式, URL)` → 文本型
- `取文本_(指针)` / `取整数_(指针)` / `取逻辑_(指针)`, `取错误_()`

**JS脚本类2_**（ActiveScript, 32位）：`执行(代码)`, `计算表达式(表达式)`, `添加对象(名称, COM对象)`

---

### 语音操作库

**包**：`火山.仰望`

**语音合成类_TTS**：
- `置语速(-10~10)`, `置声音大小(0-100)`, `置语音库(名称)`
- `机读文本(内容)` — 异步朗读
- `暂停发音/恢复发音/停止发音`
- `列举语音库()` → 文本型 | 返回可用语音名
- `输出到WAV(内容, 路径, 语速, 音量, 频率)` → 逻辑型

---

### 日志记录库

**包**：`火山.仰望`

**日志记录类_**（spdlog）：
- `创建基本日志_(名, 文件)`, `创建循环日志_(名, 文件, 最大字节, 最大文件数)`, `创建每日日志_(名, 文件, 时, 分)`
- `置日志等级_(0-5)` | 0=跟踪, 1=调试, 2=信息, 3=警告, 4=错误, 5=重要
- `添加日志_(内容, 等级, 立即保存)`, `添加日志_信息/警告/错误/重要(内容)`
- `保存日志_()`, `清空日志_()`, `加载日志_(路径)`, `筛选日志_(关键词, 结果数组)`

---

### 日期时间库

**包**：`火山.仰望`

**毫秒时间类_**：`取现行时间2_(点?)` → "2024-01-24 09:12:01.299", `取现行时间3_()` → "2024-01-24 09:12:01", `取现行时间戳_毫秒()` → 13位, `取现行时间戳_微秒()` → 16位
**纳秒时间戳类_**：`取现行时间戳_纳秒()` → 19位
**时间类_** ⭐（主要日期时间操作类，~800行）：
- `时间_取现行时间()` / `时间_取现行时间_ISO()` / `时间_取现行时间戳_文本/长整数/整数/文本`
- `时间_取指定时间戳(时间, 格式)` / `时间_时间戳到时间(时间戳)` / `时间_时间戳增减(时间戳, 增减)`
- `时间_取时间间隔(时间1, 时间2)` / `时间_取时间戳间隔(时间戳1, 时间戳2)`
- `时间_格式化时间(时间, 格式)` / `时间_秒数转时间格式(秒数, 格式)`
- `时间_取随机时间(格式, 样式)` / `时间_取随机时间戳()`
- `时间_取网络时间()` / `时间_取系统启动时间()` / `取启动时间_()`
**UTC时间类_**：UTC时间戳和转换
**格林时间类_**：GMT时间转换
**NTP网络时间类_**：通过NTP服务器获取网络时间
**时间类型_**：SYSTEMTIME结构辅助
**界面延时处理类_**：`延迟_(毫秒)` — 非阻塞带消息处理

---

### 队列操作库

**包**：`火山.仰望`

**队列类_**（线程安全环形缓冲）：
- `初始化队列_()`, `置缓冲区大小_(KB)`
- `压入字节集_(数据)` / `压入文本_(文本)` — 同通道
- `弹出字节集_(输出)` / `弹出文本_(输出)` → 逻辑型
- `取队列长度_()` → 整数
- `启用消息等待_()` — 后台线程自动触发 `队列事件(文本, 字节集)`

---

### 线程操作库

**包**：`火山.仰望`

**线程类_**：`创建线程_(参数, 立即启动)`, `等待线程_(超时)`, `暂停/继续/强制结束线程_()` + 事件 `线程事件(参数)`
**线程池类_**：`创建线程池_(数量)`, `启动线程池_(p1-p3)`, `取线程总数/已完成数()` + 事件 `线程池事件(p1-p3)`

---

### 注册表库

**包**：`火山.仰望`

**注册表类_**：
- `注册表读文本_(根节点, 键路径, 键名, 是否64位)` → 文本型
- `注册表读整数/长整数/字节集_(...)`
- `注册表写文本/整数/长整数/字节集_(根节点, 键路径, 键名, 值, 是否64位)`
- `注册表删除键值_(根节点, 键路径, 键名, 是否64位)`, `注册表枚举键/值_()`
- 根节点常量：`HKEY_CLASSES_ROOT, HKEY_CURRENT_USER, HKEY_LOCAL_MACHINE, HKEY_USERS`

---

### 硬件操作库

**包**：`火山.仰望`

**硬件综合信息类_**：`CPU_取CPU信息(制造商, 产品名, 序列号)`, `主板_取BIOS信息(...)`, `内存_取物理内存信息(...)`, `显卡_取显卡信息(...)`, `硬盘_取硬盘信息(...)`, `网卡_取网卡信息(...)` — 全部通过WMI Win32_*查询

---

### 进程权限库

**包**：`火山.仰望`

**进程类_**：`进程_结束进程(进程名)`, `进程_结束进程_批量(名)`, `进程_是否存在(名/ID)`, `进程_创建进程(路径, 参数...)`
**互斥体类_**：`关闭互斥体_路径(程序路径, 互斥体名数组)`, `关闭互斥体_ID(进程ID, 互斥体名数组)`

---

### 内存操作库

**包**：`火山.仰望`

**远程Call操作类_**：`内存_FastCall(地址, RCX-R15)` | 最多10参数, `内存_远程Call(进程ID, 汇编字节集)` | 注入shellcode
**内存操作类_**：`内存特征码搜索_(PID, 特征码, 地址数组, 起始, 结束)` | 支持`?`通配符, `内存特征码修改_(PID, 地址, 十六进制数据)`, `申请内存_(长度)` → 变整数

---

### 指针操作库

**包**：`火山.仰望`

**子程序调用类_**：`调用子程序_(方法指针, 返回值类型, ...)` | __stdcall泛型调用
**指针数据操作类_**：`取本对象指针_()`, `指针是否有效_(指针)`, `取数据指针_(变量)`, `指针到数据_(指针, 类型)`, `指针到文本_UTF8(指针)` → 文本型

---

### CMD操作库

**包**：`火山.仰望`

**CMD类_**：`运行_(命令行, 等待, 显示)`
**CMD回显类_**：`执行命令_(命令, 显示)` → 事件 `回显数据事件(字节集)`
**匿名管道类_**：`初始化_(刷新时间)`, `执行命令_(命令, UTF8)`, `终止命令_()`, `关闭命令_()` + 事件 `输出信息事件(字节集)`
**CMD代码页编码类_**：`置CMD编码_(代码页, 还原)` | `代码页编号类.UTF8=65001`, `GB2312=936`

---

### FTP操作库

**包**：`火山.仰望`

**FTP类_**：`连接FTP服务器_(地址, 用户, 密码, 端口, 被动)`, `断开FTP服务器_()`, `下载文件/上传文件_()`, `枚举目录列表_(类型, 属性, 文件名数组, 属性数组, 大小数组, 时间数组)`, `创建目录/删除目录_()`, `文件是否存在/目录是否存在_()`

---

### PDF操作库

**包**：`火山.仰望`

**PDF_Foxit类**：`加载文档(路径, 密码)`, `关闭文档()`, `取总页数()`, `读取页面(页码)`, `取页面宽度/高度()`, `画渲染页(句柄, 消息, 背景色, 左, 顶, 宽, 高)` — 在WM_PAINT中调用, `释放资源()`

---

### XML操作库

**包**：`火山.仰望`

**XML解析构造类_**（MSXML DOM）：
- `创建自文本_(XML)` / `创建自文件_(路径)` / `创建空白_(根节点名)`
- `取节点名_(XPath)` / `取节点值_(XPath)` / `置节点值_(XPath, 值)`
- `取属性值_(XPath, 属性名)` / `置属性值_(XPath, 属性名, 值)`
- `取子节点数_(XPath)` / `枚举子节点名_(XPath)`
- `插入节点_(XPath, 节点名, 值)` / `删除节点_(XPath)`
- `到源码_(编码)` → 文本型
- XPath语法：`body/data/row[0]/dm` 或 `@1/@2`

---

### 汉字拼音库

**包**：`火山.仰望`

**拼音检索库_简体**：`初始化()`, `检索数据_(匹配文本, 搜寻文本, 起始索引, 结束索引)` → 整数 | 支持多音字、拼音首字母模糊匹配

---

### 纯真IP库

**包**：`火山.仰望`

**纯真IP库类_**：`加载IP库_(路径?)`, `查询IP信息_(IP地址)` → 文本型 | "国家地区"字符串

---

### 播放器库

**包**：`火山.仰望`

**WMP播放器类**：`创建_(父句柄, 位置, 尺寸)`, `加载视频_(地址)`, `播放/暂停/停止_()`, `取播放状态/当前时间位置/媒体长度_()`, `设置音量_(0-100)`, `全屏模式_()`, `界面模式_(1-5)`

---

### 迅雷下载库

**包**：`火山.仰望`

**迅雷下载类_**（32位）：`创建任务_(URL, 保存名, Referer)` → 句柄, `创建BT任务_(种子)`, `开始/停止/删除任务_()`, `置下载速度限制_()`, `取任务信息_(句柄)` → 迅雷任务信息（状态/进度/速度/资源数等）

---

### 文件打印库

**包**：`火山.仰望`

**文件打印类_**：
- `初始化Excel打印_(结束进程?)`, `初始化Word打印_()`, `初始化PDF打印_(PDFtoPrinter路径)`
- `打印Excel_(文件, 打印机, 起页, 止页, 份数, 方向, 表索引)`
- `打印Word_(文件, 打印机, 起页, 止页, 份数, 方向)`
- `打印PDF_(文件, 打印机, 份数)`, `打印图片_(文件, 打印机, 份数, 方向, 位置, 尺寸)`

---

### 程序综合库

**包**：`火山.仰望`

**格式化类_**：`取格式数据_(文本, 格式, ...)` | swscanf风格解析
**链表排序类_**：`加入成员/插入成员(值)`, `排序(比较方法)`, `枚举循环/逆向枚举循环`, `取成员数/首/尾`, `合并排序`, `逆序`, `去重_相邻`

---

### 组件操作库

**包**：`火山.仰望`

**组件批量修改类_**：`批量修改组件布局器_(句柄, 位置, 尺寸, 可视, 禁止, 筛选条件)`, `批量修改组件布局器_底图(句柄, 底图, 放置方式)`

---

### 外部组件操作库

**包**：`火山.仰望`

**外部选择夹类_**：`取子夹数目(句柄)`, `取/置现行子夹`, `添加/插入/删除子夹`
**外部按钮类_**：`取标题(句柄)`, `置标题(句柄, 标题)`, `仿真点击(句柄)`

---

### 动态创建库

**包**：`火山.仰望`

**动态创建类**（MFC运行时控件创建）：
- `创建主窗口(标题, 宽, 高)`, `创建对话框窗口(...)`
- `创建按钮/编辑框/标签/图片框/画板/单选框/选择框/组合框/日期框/...` — 30+控件类型

---

### 动态链接库

**包**：`火山.仰望`

**动态内存加载类_**：
- `加载内存DLL_(数据指针, 数据长度)` → 变整数 | 从内存加载DLL
- `取内存函数地址_(句柄, 函数名)` → 变整数
- `释放内存DLL_(句柄)`
- `执行函数_有返回值/无返回值/无参数(地址, ...)` | 支持最多12参数

---

### 事件钩子库

**包**：`火山.仰望`

**热键监视类_**：`注册热键_(句柄, 功能键, 键代码)` → ID, `卸载热键_(句柄, ID)` + 事件 `热键触发事件(ID)`
**钩子类_**（键盘/鼠标全局钩子）：
- `注册键盘钩子_(句柄, 全局, 屏蔽, 屏蔽所有, 单次)` / `注册鼠标钩子_(...)`
- `加入屏蔽键_(键值1, 键值2?, 组合?)`, `开启拦截按键_()`
- `加入屏蔽鼠标_(操作)`, `开启拦截鼠标_()`

---

### ASM汇编库

**包**：`火山.仰望`

**ASM汇编类_**：x86/x64汇编代码生成器：
- `置代码()` — 重置, `取代码()` → 字节集
- 全部寄存器操作：`mov/push/pop/add/call/ret/je/xor/sub/lea` (EAX-ESP覆盖)
- `调用函数(进程ID, 代码)` — 远程注入执行
- `内存_FastCall(地址, RCX-R15)` — 本地调用

---

### Accessible无障碍

**包**：`火山.仰望`

**Accessible无障碍类_**（MSAA/IAccessible）：
- `创建自窗口句柄(hwnd)` / `创建自屏幕坐标(x, y)` / `创建自对象(COM对象)`
- `取名称/值/描述/角色/角色文本/状态/状态文本()`
- `取子元素总数()`, `取子元素(索引)`, `枚举子元素(数组)`, `取父元素()`, `取焦点元素()`
- `执行默认动作()`, `命中测试(x, y)` → 逻辑型, `取位置(矩形)`

---

### 数组操作库-快速排序

**包**：`火山.仰望`

**数组快速排序类_**：`排序文本_(数组, 小到大?, 附加数组, 类型)`, `排序整数_`, `排序短整数_`, `排序长整数_`, `排序小数_`, `排序字节_` + 事件 `交换数组成员事件(数组指针, i, j)`

---

## 快速查找索引

### 本技能书覆盖（67模块）

| 需要做什么 | 使用的模块/类 |
|-----------|-------------|
| 创建/操作浏览器 | FBroLib → `类_FBrowser_浏览器`, `类_FBrowser_框架` |
| 同步创建浏览器 | FBroLib → `FBrowser_创建浏览器_同步(...)` |
| 后台浏览器 | FBroLib → `FBrowser_创建后台浏览器(...)` |
| 通过标识取浏览器 | FBroLib → `FBrowser_浏览器_通过用户标识取浏览器(标识)` |
| 拦截HTTP请求/响应 | FBroEventControl → `类_FBrowser_浏览器事件` |
| 自定义资源替换 | FBroEventControl → `浏览器_获取资源处理器` + `类_FBrowser_资源处理器` |
| 修改响应内容 | FBroEventControl → `浏览器_获取资源过滤器` + `类_FBrowser_资源过滤器` |
| Cookie操作 | FBroLib → `类_FBrowser_Cookie管理器` |
| 执行JS取返回值 | `框架.执行JS代码_带返回值(代码, 回调)` + `类_FBrowser_JS回调` |
| 同步执行JS取值 | `框架.执行JS代码_带返回值(代码, 回调)` + `类_FBrowser_同步辅助类` |
| JS调用C++ | `FBrowser_JS交互_注册()` + `类_FBrowser_JS交互事件` |
| V8原生函数注册 | FBroEventControl → `渲染_即将创建V8环境` + `类_FBrowser_V8处理程序` / `类_FBrowser_V8存取器` / `类_FBrowser_V8拦截器` |
| V8 JS扩展注册 | `FBrowser_V8_注册JS扩展(名称, JS代码, V8处理程序)` |
| 访问DOM节点 | `框架.访问DOM对象(回调)` + `类_FBrowser_DOM回调`(渲染进程) |
| 填表操作 | `类_FBrowser_填表框架` → `置元素内容/取元素内容/点击元素/滚动到元素` |
| 内嵌HTTP/WS服务器 | `FBrowser_服务器_创建(...)` + `类_FBrowser_服务器事件` |
| 自定义Scheme注册 | `FBrowser_自定义方案_注册(...)` |
| 独立URL请求 | `FBrowser_创建URL请求(请求, 事件, 标识)` |
| 投递任务到UI线程 | FBroLib → `FBrowser_任务运行器_投递任务(线程ID.UI, 任务回调)` |
| 指纹伪装 | FBroVip → `类_FBrowserVIP_控制器` |
| 模拟鼠标/键盘 | FBroVip → `高级鼠标_*`, `高级键盘_*` 或 浏览器 → `发送鼠标点击事件/发送按键事件` |
| CDP截图 | FBroVip → `高级_网页截图()` |
| 谷歌模式新标签 | FBroVip → `高级_创建标签浏览器(...)` |
| 加载CRX插件 | 请求环境 → `VIP_高级_安装CRX插件包/载入插件路径` |
| 文件对话框 | 浏览器 → `打开对话框(模式, 标题, 路径, 过滤器, 回调)` |
| 右键菜单自定义 | FBroEventControl → `浏览器_即将打开菜单/菜单被点击` + `类_FBrowser_菜单模式` |
| JSON解析(yyjson) | w_yyjson → `YYJSON只读对象类` |
| JSON构造(yyjson) | w_yyjson → `YYJSON对象类` |
| HTTP服务端 | FBroEventControl → `类_FBrowser_服务器事件` |
| 网络请求(TCP/UDP) | w_network → `客户`, `数据报` |
| Base64编解码 | `FBrowser_Parser_Base64编码/解码` 或 w_bin → `字节集到BASE64文本` |
| URI编解码 | `FBrowser_Parser_URI编码/解码` |
| MD5/SHA/AES | 加密解密库 → `加解密类_`, `MD5类_` |
| 正则匹配(VBScript) | 数据匹配库 → `正则类_` |
| 字节集操作 | w_bin → `字节集类` |
| 文本操作 | w_string → `文本类` |
| 数组操作 | w_array → 模板数组类 |
| 多线程/锁 | w_muti_thread → `互斥锁类`, `线程类`, `全局线程池` |
| 文件读写 | w_misc → `磁盘操作类.读入文件/写到文件` 或 w_stream(`文件输入/输出流`) |
| 启动时间/延时 | w_misc → `常用功能类.取启动时间()` / `常用功能类.延时()` |
| 调试输出 | w_misc → `程序调试类.调试输出(...)` |
| 时间/日期操作 | w_misc → `时间操作类.取现行时间/取时间戳/到时间` |
| 哈希表 | w_misc → `通用哈希表模板类` |
| 运行外部程序 | w_misc → `常用功能类.运行(命令行, ...)` |
| SQLite数据库 | w_sqlite3 → `SQLite数据库类` |
| 日志(spdlog) | 日志记录库 → `日志记录类_` |
| 二维码 | 条码二维码库 → `二维码类_` |
| OCR识别 | OCR识别库 → `RapidOCR识别类_` |
| 语音朗读 | 语音操作库 → `语音合成类_TTS` |
| XML操作 | XML操作库 → `XML解析构造类_` |
| 注册表 | 注册表库 → `注册表类_` |
| 事件智能指针 | FBroLib → `FBrowser创建事件智能指针(类型)` / `FBrowser创建类指针(类)` |
| 同步转异步等待 | FBroCallback → `类_FBrowser_同步辅助类` |
| DPI设置 | FBroLib → `FBrowser_设置程序DPI模式(模式)` |
| COM操作 | w_com → `COM对象类` |

### 扩展技能书覆盖 → [volcano-core-ext.md](volcano-core-ext.md)

| 需要做什么 | 使用的模块/类 |
|-----------|-------------|
| ADO数据库(MySQL/MSSQL/PG/Access) | w_ado → `数据库类` |
| AI对话(阿里云百炼/DeepSeek) | w_AI → `阿里云问答类` |
| C++正则匹配(std::regex) | w_regex → `正则表达式类` |
| OpenSSL AES/RSA/MD5/SHA | w_openssl → `加解密类` |
| HTTP/HTTPS请求(curl) | w_curl → `网络传输类` |
| 文件上传/下载(curl) | w_curl → `网络传输类.启用文件上传` |
| 编码转换(任意编码互转) | w_iconv → `编码转换类` |
| JSON操作(jsoncpp) | w_json → `JSON对象类` / `JSON数组类` |
| HTML DOM解析(gumbo) | w_html_parser → `网页解析器` |
| HTML CSS选择器查询 | w_pro_html → `高级网页解析器` |
| 日志(plog文件/控制台) | w_plog → `日志类` / `日志输出` |

### 扩展技能书覆盖 → [volcano-ext-libs.md](volcano-ext-libs.md)

| 需要做什么 | 使用的模块/类 |
|-----------|-------------|
| MFC窗口/控件开发 | w_mfc_ui_base → `窗口组件` |
| MFC表格组件 | w_mfc_ui_grid → `表格` |
| MFC多文档MDI | w_mfc_ui_mdi → `MDI子窗口` |
| MFC属性表格 | w_mfc_property_grid → `属性表格` |
| ODBC数据库 | w_mfc_odbc → `外部数据库` |
| WTL窗口开发 | w_wtl_base_control → `窗口` |
| HP-Socket高性能TCP | w_hp_socket_client/server |
| HP-Socket HTTP服务器 | w_hp_socket_http |
| WebSocket客户端 | w_hp_websocket → `Websocket客户端类2` |
| Redis数据库 | w_hiredis → `Redis数据库类` |
| V8 JS引擎 | w_google_v8 → `JS引擎类` |
| ELK嵌入式JS | w_elk_js → `ELK引擎类` |
| Python集成 | w_cvolpy → `火山Python` |
| FTP服务器 | w_ftp_server → `FTP服务器类` |
| POP3邮件接收 | w_email_receiver → `邮件接收类` |
| 多格式图像处理 | w_cx_image → `图像处理类` |
| 位图处理(旋转/缩放) | w_adv_bitmap → `增强位图类` |
| 摄像头/视频播放 | w_directshow → `摄像头类` |
| MMKV键值存储 | w_mmkv → `高性能键值表` |
| 树结构数据 | w_TreeStruct → `树结构类` |
| ZIP压缩解压(libzip) | libzip → `ZIP压缩解压类` |
| Cron表达式 | w_Croncpp → `时间表达式类` |
| DEELX正则 | w_deelx_regular → `正则表达式类` |
| 内存加载DLL/EXE | w_memory_module → `内存模块` |
| 串口通信 | w_CSerialPort → `串口通信类` |
| 崩溃dump | w_breakpad → `程序崩溃处理类` |
| 阿里云OSS | w_aliyun_oss → `阿里云对象储存类` |
| 百度网盘 | w_baidu_Netdisk → `百度网盘` |
| 海康工业相机 | w_hik_machine → `工业相机类` |

### 补充技能书覆盖 → [volcano-supplementary.md](volcano-supplementary.md)

| 需要做什么 | 使用的模块/类 |
|-----------|-------------|
| MFC超级菜单/卷帘菜单 | w_mfc_adv_menu / w_mfc_out_bar |
| Scintilla代码编辑器 | w_scintilla / w_wtl_scintilla |
| ECharts图表 | mfc_ui_echarts_control |
| IGR锐浪报表 | w_IGRComponent → `报表` |
| XCGUI炫彩界面 | xcgui → `炫彩基类` |
| MiniBlink浏览器 | mfc_ui_mb / vMiniBlink |
| WebView2/Edge | mfc_ui_webview2 |
| MySQL数据库 | w_mysql → `Mysql数据库类` |
| WinHTTP请求 | w_WinHTTP5_1 / COM_WinHttp |
| WebSocket(多版本) | WebSocket / ixwebsocket / HPWebsocket |
| KCP/nanomsg/libhv | kcp / nanomsg / libhv |
| LevelDB存储 | leveldbTool |
| Lua脚本 | Lua |
| Tesseract OCR | TesseractOcr |
| 结巴中文分词 | cppjieba |
| aria2下载 | aria2 |
| API Hook | Detours |
| 音频处理 | SoundTouch / WebRTCNS / G729A |
| Excel/Word/PPT COM | ExcelClassInterface / WordClassInterface / PowerPointClassInterface |
| YAML解析 | w_yaml_cpp |
| pugixml XML | w_Pugixml |
| RAR解压 | w_unrar |
| Toast通知 | w_win_toast |
| 任务栏进度 | w_taskbar_list |
| mimalloc内存 | w_mimolloc |
| STL模板 | std_vector / std_map / std_list |
| 微信支付 | Lkuaiypay |
| 标准线程池 | ThreadPoolCPP |

---

> 📘 **相关技能**：
> - [volcano-core-ext.md](volcano-core-ext.md) — 核心扩展模块（ADO/AI/正则/OpenSSL/cURL/iconv/JSON/HTML/plog）
> - [volcano-ext-libs.md](volcano-ext-libs.md) — 扩展库（MFC/WTL/HP-Socket/Redis/V8/图像/MMKV/云服务）
> - [yyjson-api.md](yyjson-api.md) — yyjson JSON库完整API参考
> - [CLAUDE.md](CEFbro\CLAUDE.md) — 火山视窗语法速查 + MCP Server架构
