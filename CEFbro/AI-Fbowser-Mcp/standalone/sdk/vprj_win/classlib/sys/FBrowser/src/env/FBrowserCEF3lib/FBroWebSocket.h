#pragma once
#ifndef FBROWSER_WEBSOCKET_H_
#define FBROWSER_WEBSOCKET_H_

//extern HANDLE websocket_hMap;
//extern Pointer_VIP_WebSocket_data websocket_pShareMem;

//void WebSocketDis(CefRefPtr<CefBrowser> browser, CefRefPtr<CefV8Context> context, string mainpid);
//
//
////虚拟事件
////渲染进程中执行,如果不加TEXPORTS，执行后会崩溃，原因未知
//void TEXPORTS OnWebSocketCreate(int bid, int64_t fid, HANDLE wssPtr);
//void TEXPORTS OnWebSocketClose( int bid, int64_t fid, HANDLE wssPtr);
//void TEXPORTS OnWebSocketConnect( int bid, int64_t fid, HANDLE wssPtr, void*& returl, void*& protocols);
//bool TEXPORTS OnWebSocketSend( int bid, int64_t fid, HANDLE wssPtr, int type, void*& returl, int offsize, int& size);
//bool TEXPORTS OnWebSocketMessage( int bid, int64_t fid, HANDLE wssPtr, int type, void*& data, int& size);

#endif